---
import { Image } from 'astro:assets'
// =====================================================================
// COMPONENT IMPORTS - CLEAN AND SIMPLE
// =====================================================================
import Footer from '@components/Footer.astro'
import Navbar from '@components/Navbar.astro'
import BackToTop from '@components/control/BackToTop.astro'
import RightRailContent from '@components/widget/RightRailContent.astro'
import SideBar from '@components/widget/SideBar.astro'
import type { MarkdownHeading } from 'astro'
import { Icon } from 'astro-icon/components'
import ImageWrapper from '../components/misc/ImageWrapper.astro'
import TimelineController from '../components/timeline/TimelineBanner.astro'
import TOC from '../components/widget/TOC.astro'
import { siteConfig } from '@/config/config'
import Layout from './Layout.astro'
import '../styles/timeline-styles.css'
import { pathsEqual, url } from '../utils/url-utils'

// Blog-core shared client components (relative imports)
import MobileProfileOverlay from '../components/client/MobileProfileOverlay.svelte'
import PageAnimations from '../components/client/PageAnimations.svelte'
import Profile from '../components/client/Profile.svelte'
import ScrollEffects from '../components/client/ScrollEffects.svelte'
import SidenavPanels from '../components/client/SidenavPanels.svelte'
import SpecialPageFeatures from '../components/client/SpecialPageFeatures.svelte'

import { avatarConfig } from '@/config/avatar.config'
// Profile and avatar configs for Svelte components
import { profileConfig } from '@/config/config'

// App-level banner configuration (supports both legacy and advanced contracts)
import * as bannerModule from '@/config/banner.config'

// =====================================================================
// TYPESCRIPT INTERFACES
// =====================================================================

interface Props {
  title?: string
  banner?: string
  description?: string
  lang?: string
  setOGTypeArticle?: boolean
  headings?: MarkdownHeading[]
  post?: any
  bannerLink?: string
  isPostPage?: boolean
  pageMascotContext?: string
  backgroundImage?: string | null
  // ⭐ NEW: OneColumn layout prop
  oneColumn?: boolean
  // ⭐ NEW: Custom author data prop
  customAuthorData?: {
    avatarImage?: string
    authorName?: string
    authorBio?: string
    authorLink?: string
  }
  homeLayout?: 'default' | 'portal'
  contentMode?: 'default' | 'banner-only'
  showAssistantWidget?: boolean
  timelineEvents?: any[]
  timelineEraConfig?: Record<string, any>
  timelineConstellationConfig?: Record<string, any>
  timelineConstellationPatterns?: Record<string, any>
  timelineConnectionPatterns?: Record<string, any>
}

// =====================================================================
// PROPS DESTRUCTURING AND SETUP
// =====================================================================

const {
  title,
  banner = siteConfig.banner.src,
  description,
  lang,
  setOGTypeArticle,
  headings = [],
  post,
  bannerLink = '',
  isPostPage = false,
  backgroundImage,
  oneColumn = false, // ⭐ NEW: Accept oneColumn prop
  customAuthorData, // ⭐ NEW: Extract custom author data
  homeLayout = 'default',
  contentMode = 'default',
  showAssistantWidget = false,
  timelineEvents = [],
  timelineEraConfig,
  timelineConstellationConfig,
  timelineConstellationPatterns,
  timelineConnectionPatterns,
} = Astro.props

const isBannerOnly = contentMode === 'banner-only'

const rawBannerConfig = (bannerModule as any).bannerConfig ?? {}

function createLegacyStandardBannerConfig(config: any) {
  const bannerList = Array.isArray(config.bannerList) ? config.bannerList : []
  const bannerLinks = Array.isArray(config.bannerLinks) ? config.bannerLinks : []

  const getBannerLinkFromList = (index: number): string | null => {
    if (typeof bannerLinks[index] !== 'string') return null
    const link = bannerLinks[index].trim()
    return link.length > 0 ? link : null
  }

  return {
    bannerList,
    bannerLinks,
    animation: {
      enabled: config.animation?.enabled ?? true,
      interval: config.animation?.interval ?? 5000,
      transitionDuration: config.animation?.transitionDuration ?? 1000,
      direction: config.animation?.direction ?? 'forward',
    },
    video: {
      autoplay: true,
      muted: true,
      loop: true,
      playsInLine: true,
      controls: false,
      preload: 'none',
    },
    getBannerAnimationSettings: () => ({
      enabled: config.animation?.enabled ?? true,
      interval: config.animation?.interval ?? 5000,
      transitionDuration: config.animation?.transitionDuration ?? 1000,
      direction: config.animation?.direction ?? 'forward',
    }),
    getBannerLink: getBannerLinkFromList,
    hasAnyBannerLinks: () => bannerLinks.some((link: unknown) => typeof link === 'string' && link.trim().length > 0),
    getLinkPreviewData: (url: string) => ({
      title: 'Explore More',
      description: url || 'Click to visit this page',
      icon: 'arrow-up-right-from-square',
    }),
    getIconSVG: () => '',
    getBannerItemPreviewDetails: (index: number) => {
      const item = bannerList[index]
      const link = getBannerLinkFromList(index)
      return {
        hasValidLink: !!link,
        originalHref: link || '',
        urlForDisplay: link || '',
        previewTitle: item?.alt || `Banner ${index + 1}`,
        previewDescription: 'Click to explore this banner item.',
        previewIconSVG: '',
        isVideoButton: item?.type === 'video',
      }
    },
    isVideoBannerItem: (item: any) =>
      item?.type === 'video' ||
      (typeof item?.src === 'string' && /\.(webm|mp4|mov|ogg)(\?.*)?$/i.test(item.src)),
    isImageBannerItem: (item: any) =>
      item?.type === 'image' ||
      (typeof item?.src !== 'undefined' && item?.type !== 'video'),
  }
}

function normalizeCssSize(
  value: unknown,
  fallback: string,
  defaultUnit: 'vh' | 'rem' = 'vh',
): string {
  if (typeof value === 'number' && Number.isFinite(value)) {
    return value === 0 ? '0' : `${value}${defaultUnit}`
  }

  if (typeof value !== 'string') return fallback

  const trimmedValue = value.trim()
  if (!trimmedValue) return fallback

  // Keep valid CSS expressions as-is.
  if (
    trimmedValue.startsWith('calc(') ||
    trimmedValue.startsWith('clamp(') ||
    trimmedValue.startsWith('min(') ||
    trimmedValue.startsWith('max(') ||
    trimmedValue.startsWith('var(')
  ) {
    return trimmedValue
  }

  // Accept common CSS size units directly.
  if (
    /^-?\d*\.?\d+(px|rem|em|vh|vw|vmin|vmax|svh|lvh|dvh|%)$/i.test(
      trimmedValue,
    )
  ) {
    return trimmedValue
  }

  // Handle bare numbers like "80" from manual tuning.
  if (/^-?\d*\.?\d+$/.test(trimmedValue)) {
    const numericValue = Number(trimmedValue)
    return numericValue === 0 ? '0' : `${trimmedValue}${defaultUnit}`
  }

  return fallback
}

function normalizeBannerConfig(config: any) {
  const standardBannerConfig =
    config.standardBannerConfig ?? createLegacyStandardBannerConfig(config)

  const rawDesktopBannerHeight =
    typeof config.layout?.height === 'string'
      ? config.layout.height
      : config.layout?.height?.desktop
  const desktopBannerHeight = normalizeCssSize(rawDesktopBannerHeight, '60vh')

  const rawMobileBannerHeight =
    config.layout?.mobileHeight ??
    (typeof config.layout?.height === 'object'
      ? config.layout?.height?.mobile
      : undefined)
  const mobileBannerHeight = normalizeCssSize(
    rawMobileBannerHeight,
    desktopBannerHeight,
  )

  const spacingFromConfig = config.navbar?.spacing ?? config.navbarSpacing ?? {}
  const mobileBannerGap =
    config.navbar?.mobileBannerGap ??
    config.navbar?.mobilePortraitSpacing ??
    config.navbar?.height?.mobile ??
    '4rem'
  const mobilePortraitPanelTop =
    config.panel?.top?.mobilePortrait ??
    config.panel?.top?.standard ??
    '0'

  return {
    ...config,
    defaultBannerType: config.defaultBannerType ?? 'standard',
    defaultBannerData: config.defaultBannerData ?? {},
    standardBannerConfig,
    layout: {
      ...config.layout,
      height: desktopBannerHeight,
      mobileHeight: mobileBannerHeight,
      maxWidth: config.layout?.maxWidth ?? 1920,
      mainContentOffset: config.layout?.mainContentOffset ?? '1.5rem',
    },
    visual: {
      objectFit: config.visual?.objectFit ?? 'cover',
      objectPosition: config.visual?.objectPosition ?? 'center',
      applyGradientOverlay: config.visual?.applyGradientOverlay ?? false,
      gradientOverlay:
        config.visual?.gradientOverlay ??
        'linear-gradient(to bottom, rgba(0,0,0,0.2), transparent)',
      borderRadius: config.visual?.borderRadius ?? '0',
    },
    fallback: {
      enabled: config.fallback?.enabled ?? true,
      type: config.fallback?.type ?? 'gradient',
      value:
        config.fallback?.value ??
        'linear-gradient(to bottom, var(--color-primary-light), var(--color-primary))',
    },
    navbar: {
      ...config.navbar,
      height:
        typeof config.navbar?.height === 'string'
          ? config.navbar.height
          : config.navbar?.height?.desktop ?? '4.5rem',
      mobileBannerGap,
      // Backward compatibility: older configs may still read/write this key.
      mobilePortraitSpacing: mobileBannerGap,
      spacing: {
        standard: spacingFromConfig.standard ?? '0',
        timeline: spacingFromConfig.timeline ?? '5.5rem',
        video: spacingFromConfig.video ?? '5.5rem',
        image: spacingFromConfig.image ?? '0',
        assistant: spacingFromConfig.assistant ?? spacingFromConfig.standard ?? '0',
        none: spacingFromConfig.none ?? '0',
      },
    },
    panel: {
      ...config.panel,
      top: {
        standard: config.panel?.top?.standard ?? '0',
        timeline: config.panel?.top?.timeline ?? '0',
        video: config.panel?.top?.video ?? '0',
        image: config.panel?.top?.image ?? '0',
        assistant: config.panel?.top?.assistant ?? config.panel?.top?.standard ?? '0',
        none: config.panel?.top?.none ?? '0',
        mobilePortrait: mobilePortraitPanelTop,
      },
    },
    parallax: {
      enabled: config.parallax?.enabled ?? false,
      scrollFactor: config.parallax?.scrollFactor ?? -0.02,
      easingFactor: config.parallax?.easingFactor ?? 0.1,
    },
  }
}

const bannerConfig = normalizeBannerConfig(rawBannerConfig)
const baseUrl = (import.meta.env.BASE_URL || '/').replace(/\/$/, '')

function normalizeAssetUrl(assetPath?: string | null): string {
  if (!assetPath) return ''
  if (
    assetPath.startsWith('http://') ||
    assetPath.startsWith('https://') ||
    assetPath.startsWith('//') ||
    assetPath.startsWith('data:')
  ) {
    return assetPath
  }
  if (!assetPath.startsWith('/')) return assetPath
  if (baseUrl && assetPath.startsWith(`${baseUrl}/`)) return assetPath
  return url(assetPath, import.meta.env.BASE_URL)
}

const getPanelTopPosition = (bannerType: string): string => {
  switch (bannerType) {
    case 'video':
      return bannerConfig.panel.top.video
    case 'image':
      return bannerConfig.panel.top.image
    case 'timeline':
      return bannerConfig.panel.top.timeline
    case 'assistant':
      return bannerConfig.panel.top.assistant
    case 'none':
      return bannerConfig.panel.top.none
    default:
      return bannerConfig.panel.top.standard
  }
}

const determineBannerConfiguration =
  typeof (bannerModule as any).determineBannerConfiguration === 'function'
    ? (bannerModule as any).determineBannerConfiguration
    : (postValue: any, _pageType = 'default', defaultBannerLink = '') => {
        const postData = {
          bannerLink: postValue?.data?.bannerLink || '',
          customAvatar: normalizeAssetUrl(postValue?.data?.avatarImage || ''),
          customName: postValue?.data?.authorName || '',
          customBio: postValue?.data?.authorBio || '',
          slug: postValue?.slug || '',
          // `showImageOnPost` only controls the inline article cover image.
          // It should not disable the page banner or shared default banner system.
          wantsNoDefaultBanner: false,
        }

        const hasPostTimelineBanner =
          postValue?.data?.bannerType === 'timeline' &&
          postValue?.data?.bannerData?.category
        const hasPostVideoBanner =
          postValue?.data?.bannerType === 'video' &&
          postValue?.data?.bannerData?.videoId
        const hasPostAssistantBanner = postValue?.data?.bannerType === 'assistant'
        const hasPostImageBanner =
          !!(
            postValue?.data?.image ||
            (postValue?.data?.bannerType === 'image' &&
              postValue?.data?.bannerData?.imageUrl)
          ) &&
          !hasPostVideoBanner &&
          !hasPostTimelineBanner &&
          !hasPostAssistantBanner

        const hasPostBanner =
          hasPostVideoBanner ||
          hasPostImageBanner ||
          hasPostTimelineBanner ||
          hasPostAssistantBanner

        const defaultBannerType = bannerConfig.defaultBannerType
        const hasTimelineBanner =
          hasPostTimelineBanner || (!hasPostBanner && defaultBannerType === 'timeline')
        const hasVideoBanner =
          hasPostVideoBanner || (!hasPostBanner && defaultBannerType === 'video')
        const hasImageBanner =
          hasPostImageBanner || (!hasPostBanner && defaultBannerType === 'image')
        const hasAssistantBanner =
          hasPostAssistantBanner ||
          (!hasPostBanner && defaultBannerType === 'assistant')
        const hasStandardBanner =
          !hasPostBanner &&
          !hasTimelineBanner &&
          !hasVideoBanner &&
          !hasImageBanner &&
          !hasAssistantBanner

        const currentBannerType = hasVideoBanner
          ? 'video'
          : hasImageBanner
            ? 'image'
            : hasTimelineBanner
              ? 'timeline'
              : hasAssistantBanner
                ? 'assistant'
                : hasStandardBanner
                  ? 'standard'
                  : 'none'

        const postImage = normalizeAssetUrl(
          postValue?.data?.bannerData?.imageUrl || postValue?.data?.image,
        )

        return {
          postData,
          bannerType: {
            hasTimelineBanner,
            hasVideoBanner,
            hasImageBanner,
            hasAssistantBanner,
            hasStandardBanner,
            hasPostBanner,
            isStandardPage: !hasPostBanner,
            currentBannerType,
          },
          bannerDataSources: {
            videoBannerData:
              hasVideoBanner && postValue?.data?.bannerType === 'video'
                ? postValue.data.bannerData
                : hasVideoBanner
                  ? bannerConfig.defaultBannerData
                  : null,
            imageBannerData: hasImageBanner
              ? postImage
                ? { imageUrl: postImage }
                : bannerConfig.imageBannerConfig?.data || null
              : null,
            timelineBannerData:
              hasTimelineBanner && postValue?.data?.bannerType === 'timeline'
                ? postValue.data.bannerData
                : hasTimelineBanner
                  ? bannerConfig.defaultBannerData
                  : null,
            assistantBannerData:
              hasAssistantBanner && postValue?.data?.bannerType === 'assistant'
                ? postValue.data.bannerData
                : hasAssistantBanner
                  ? bannerConfig.defaultBannerData
                  : null,
          },
          layout: {
            mainPanelTop: getPanelTopPosition(currentBannerType),
            navbarSpacing:
              bannerConfig.navbar.spacing[currentBannerType] ??
              bannerConfig.navbar.spacing.standard,
            bannerHeight: bannerConfig.layout.height,
            bannerHeightMobile: bannerConfig.layout.mobileHeight ?? bannerConfig.layout.height,
            mainContentOffset: bannerConfig.layout.mainContentOffset,
          },
          finalBannerLink: postData.bannerLink || defaultBannerLink,
          currentBannerType,
        }
      }

const getBannerAnimationSettings =
  typeof (bannerModule as any).getBannerAnimationSettings === 'function'
    ? (bannerModule as any).getBannerAnimationSettings
    : () => bannerConfig.standardBannerConfig.getBannerAnimationSettings()

const getBannerLink =
  typeof (bannerModule as any).getBannerLink === 'function'
    ? (bannerModule as any).getBannerLink
    : (index: number) => bannerConfig.standardBannerConfig.getBannerLink(index)

const getBannerItemPreviewDetails =
  typeof (bannerModule as any).getBannerItemPreviewDetails === 'function'
    ? (bannerModule as any).getBannerItemPreviewDetails
    : (index: number) =>
        bannerConfig.standardBannerConfig.getBannerItemPreviewDetails(index)

const getFallbackBannerCSS =
  typeof (bannerModule as any).getFallbackBannerCSS === 'function'
    ? (bannerModule as any).getFallbackBannerCSS
    : () =>
        bannerConfig.fallback.enabled
          ? bannerConfig.fallback.value
          : ''

const getDynamicBackgroundImage =
  typeof (bannerModule as any).getDynamicBackgroundImage === 'function'
    ? (bannerModule as any).getDynamicBackgroundImage
    : (bgImage?: string | null) => {
        if (bgImage === 'none' || bgImage === '') return null
        if (bgImage) return bgImage
        if (siteConfig?.banner?.enable && siteConfig?.banner?.src) {
          return siteConfig.banner.src
        }
        return null
      }

const getShouldShowParallaxBackground =
  typeof (bannerModule as any).getShouldShowParallaxBackground === 'function'
    ? (bannerModule as any).getShouldShowParallaxBackground
    : (bgImage?: string | null) =>
        !!(getDynamicBackgroundImage(bgImage) && bannerConfig.parallax.enabled)

// =====================================================================
// ⭐ NEW: Determine oneColumn mode from props or post data
// =====================================================================
const oneColumnMode = oneColumn || post?.data?.oneColumn || false
if (import.meta.env.DEV) {
}

// =====================================================================
// ⭐ RESTORED: Use working banner configuration system
// =====================================================================

// Get current path for page type determination
const currentPath = Astro.url.pathname
const basePath = import.meta.env.BASE_URL || '/'
const isHomePage = pathsEqual(currentPath, basePath)
const isPortalHome = isHomePage && homeLayout === 'portal'
const isPostRoute = Boolean(post)

// ⭐ RESTORED: Use the working banner configuration function
const bannerConfiguration = determineBannerConfiguration(
  post,
  'default',
  bannerLink,
)

const bannerAnimationSettings = getBannerAnimationSettings()

// Destructure the processed configuration - WORKING STRUCTURE
const {
  postData,
  bannerType,
  bannerDataSources,
  layout,
  finalBannerLink,
  currentBannerType,
} = bannerConfiguration

const tocMinHeadings = siteConfig.toc.minHeadings ?? 3
const tocOverride =
  typeof post?.data?.showToc === 'boolean' ? post.data.showToc : undefined
const hasEnoughHeadingsForToc = headings.length >= tocMinHeadings
const shouldShowToc =
  siteConfig.toc.enable && (tocOverride ?? hasEnoughHeadingsForToc)

const shouldShowFallbackRightRail =
  !isPortalHome &&
  siteConfig.rightRail?.enable === true &&
  ((isHomePage && siteConfig.rightRail?.showOnHome !== false) ||
    (isPostRoute &&
      !shouldShowToc &&
      siteConfig.rightRail?.showOnPostsWithoutToc === true))

const shouldShowRightRail = shouldShowToc || shouldShowFallbackRightRail
const shouldMergeRightRailIntoSidebar = shouldShowFallbackRightRail && !shouldShowToc
const rightRailStickyTop = siteConfig.rightRail?.stickyTop ?? '3.5rem'

// Process custom author data with direct post-data fallbacks so avatar overrides
// remain functional even if app banner adapters omit author fields.
const directPostAuthorData = {
  slug: post?.slug || '',
  avatarImage: normalizeAssetUrl(post?.data?.avatarImage || ''),
  authorName: post?.data?.authorName || '',
  authorBio: post?.data?.authorBio || '',
  authorLink: post?.data?.authorLink || '',
}

const processedAuthorData = {
  slug: postData?.slug || directPostAuthorData.slug || '',
  customAvatar: normalizeAssetUrl(
    customAuthorData?.avatarImage ||
      postData?.customAvatar ||
      directPostAuthorData.avatarImage ||
      '',
  ),
  customName:
    customAuthorData?.authorName ||
    postData?.customName ||
    directPostAuthorData.authorName ||
    '',
  customBio:
    customAuthorData?.authorBio ||
    postData?.customBio ||
    directPostAuthorData.authorBio ||
    '',
  customLink:
    customAuthorData?.authorLink ||
    postData?.customLink ||
    directPostAuthorData.authorLink ||
    '',
}

if (import.meta.env.DEV) {
}

// Destructure banner type flags
const {
  hasTimelineBanner,
  hasVideoBanner,
  hasImageBanner,
  hasAssistantBanner,
  hasStandardBanner,
  hasPostBanner,
  isStandardPage,
} = bannerType

// ⭐ RESTORED: Use working layout values
const {
  mainPanelTop, // This controls the overlap (WORKING)
  navbarSpacing, // This controls navbar spacing (WORKING + MOBILE PORTRAIT FIX)
  bannerHeight,
  bannerHeightMobile,
  mainContentOffset,
} = layout

const resolvedBannerHeight = normalizeCssSize(
  bannerHeight,
  bannerConfig.layout.height,
)
const resolvedBannerHeightMobile = normalizeCssSize(
  bannerHeightMobile,
  bannerConfig.layout.mobileHeight ?? resolvedBannerHeight,
)

const mobileMainPanelTop =
  currentBannerType === 'standard'
    ? (bannerConfig.panel?.top?.mobilePortrait ?? mainPanelTop)
    : mainPanelTop
const mobilePortraitRightRailHeroStyle =
  siteConfig.rightRail?.mobilePortraitHero?.style ?? 'banner-overlay'
const shouldShowMobilePortraitRightRailHero =
  shouldMergeRightRailIntoSidebar &&
  currentBannerType === 'standard' &&
  siteConfig.rightRail?.mobilePortraitHero?.enabled === true

// =====================================================================
// ⭐ RESTORED: Background image logic (working)
// =====================================================================

const currentBackgroundImage = getDynamicBackgroundImage(backgroundImage)
const shouldShowParallaxBackground =
  getShouldShowParallaxBackground(backgroundImage)

// =====================================================================
// BLEEPY CONTEXT SETUP
// =====================================================================

let contextForBleepy = null
if (post && post.data) {
  contextForBleepy = {
    title: post.data.title,
    description: post.data.description,
    tags: post.data.tags,
    category: post.data.category,
    timelineEra: post.data.timelineEra,
    timelineLocation: post.data.timelineLocation,
    mascotContext: post.data.mascotContext,
  }
}

// Conditionally create the grid classes
const gridLayoutClasses = [
  'transition duration-700 w-full left-0 right-0 grid mx-auto gap-4 md:gap-4 px-2 md:px-4 relative',
  isPortalHome || isBannerOnly ? 'grid-cols-1' : 'grid-cols-1 md:grid-cols-[16.5rem_auto]',
  !isPortalHome && !isBannerOnly && shouldShowRightRail
    ? '2xl:grid-cols-[16.5rem_auto_var(--toc-width)]'
    : '',
].join(' ')
---

<Layout 
  title={title} 
  banner={banner} 
  description={description} 
  lang={lang} 
  setOGTypeArticle={setOGTypeArticle}
  post={post} 
>

  <!-- =============================================================== -->
  <!-- PARALLAX BACKGROUND SECTION                                   -->
  <!-- =============================================================== -->
  {shouldShowParallaxBackground &&
    <div id="parallax-banner" class="fixed top-0 left-0 w-full h-screen z-[-1] overflow-hidden">
      {siteConfig.banner.video ? (
        <>
          <video
            id="parallax-image"
            class="parallax-bg object-cover w-full h-[120vh]"
            autoplay
            loop
            muted
            playsinline
            aria-hidden="true"
            data-playback-rate={siteConfig.banner.playbackRate ?? 1}
            style={`object-position: ${siteConfig.banner.position || 'center'}`}
          >
            <source src={siteConfig.banner.video} type="video/webm" />
          </video>
          <script>
            (function () {
              var v = document.getElementById('parallax-image');
              if (v && v.tagName === 'VIDEO') {
                var configuredRate = Number(v.dataset.playbackRate);
                var playbackRate = Number.isFinite(configuredRate) && configuredRate > 0 ? configuredRate : 1;
                v.playbackRate = playbackRate;
                v.addEventListener('play', function () { v.playbackRate = playbackRate; });
              }
            })();
          </script>
        </>
      ) : (
        <ImageWrapper
          id="parallax-image"
          alt="Site background image"
          class="parallax-bg object-cover w-full h-[120vh]"
          src={currentBackgroundImage}
          position={siteConfig.banner.position || 'center'}
        />
      )}
      {siteConfig.banner.credit?.enable &&
        <div class="absolute bottom-4 right-4 text-white/50 text-xs bg-black/30 px-2 py-1 rounded">
          {siteConfig.banner.credit.text &&
            <a href={siteConfig.banner.credit.url || '#'} class="hover:text-white/80 transition">
              {siteConfig.banner.credit.text}
            </a>
          }
        </div>
      }
      <div class="absolute inset-0 bg-gradient-to-b from-black/20 to-black/50 pointer-events-none"></div>
    </div>
  }

  <!-- =============================================================== -->
  <!-- ⭐ SIMPLIFIED: NAVBAR - NOW ALWAYS VISIBLE                     -->
  <!-- =============================================================== -->
  <slot slot="head" name="head"></slot>
  <div id="top-row" class="z-50 pointer-events-none relative transition-all duration-700 max-w-[var(--page-width)] px-2 md:px-4 mx-auto">
    <div id="navbar-wrapper" class="pointer-events-auto sticky top-0 transition-all duration-300">
      <Navbar></Navbar>
    </div>
  </div>

  <!-- =============================================================== -->
  <!-- ⭐ SIMPLIFIED: Banner container - TAILWIND ONLY               -->
  <!-- =============================================================== -->
  <div id="banner-container" 
    class={`z-10 w-full relative transition-all duration-500
      ${hasTimelineBanner ? 'has-timeline-banner' : ''}
      ${hasVideoBanner ? 'has-video-banner' : ''}
      ${hasImageBanner ? 'has-image-banner' : ''}
      ${hasAssistantBanner ? 'has-assistant-banner' : ''}
      ${hasStandardBanner ? 'has-standard-banner' : ''}
      ${isBannerOnly ? 'banner-only-mode' : ''}`}
    data-mobile-right-rail-hero={shouldShowMobilePortraitRightRailHero ? mobilePortraitRightRailHeroStyle : 'default'}
  >

    <!-- VIDEO BANNER -->
    {hasVideoBanner && (
      <div class="banner-container-video">
        <div class="banner-aspect-container">
          <div class="banner-content-wrapper">
            <iframe
              src={`https://www.youtube.com/embed/${hasPostBanner && post?.data?.bannerType === "video" ? post.data.bannerData.videoId : bannerDataSources.videoBannerData?.videoId}?autoplay=0&mute=0&controls=1&enablejsapi=1&playsinline=1`}
              title="YouTube video player"
              frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture;"
              allowfullscreen
              class="absolute top-0 left-0 w-full h-full"
            ></iframe>
          </div>
        </div>
      </div>
    )}

    <!-- ⭐ NEW: TIMELINE BANNER WITH MOBILE INTERACTION WRAPPER -->
    {hasTimelineBanner && (
      <div class:list={['banner-container-timeline', isBannerOnly ? 'banner-container-timeline--hero-only' : '']}>
        <div class="banner-aspect-container">
          <div class:list={['banner-content-wrapper', isBannerOnly ? 'banner-content-wrapper--timeline-hero' : '']}>
            <!-- ⭐ NEW: Mobile interaction wrapper -->
            <div id="timeline-mobile-wrapper" class="timeline-mobile-inactive">
              <!-- ⭐ NEW: Mobile tap indicator -->
              <div class="timeline-mobile-indicator md:hidden">
                <div class="timeline-tap-hint">
                  <Icon name="fa6-solid:hand-pointer" class="w-6 h-6 text-white/80" />
                  <span class="text-white/80 text-sm font-medium">Tap to interact with timeline</span>
                </div>
              </div>
              
              <TimelineController
                category={hasPostBanner && post?.data?.bannerType === "timeline" ? post.data.bannerData.category : (bannerDataSources.timelineBannerData?.category || "MEGA MEAL")}
                startYear={hasPostBanner && post?.data?.bannerType === "timeline" ? post.data.bannerData.startYear : bannerDataSources.timelineBannerData?.startYear}
                endYear={hasPostBanner && post?.data?.bannerType === "timeline" ? post.data.bannerData.endYear : bannerDataSources.timelineBannerData?.endYear}
                background={hasPostBanner && post?.data?.bannerType === "timeline" ? post.data.bannerData.background : bannerDataSources.timelineBannerData?.background}
                compact={hasPostBanner && post?.data?.bannerType === "timeline" ? (post.data.bannerData.compact || false) : (bannerDataSources.timelineBannerData?.compact || false)}
                events={timelineEvents}
                eraConfig={timelineEraConfig}
                constellationConfig={timelineConstellationConfig}
                constellationPatterns={timelineConstellationPatterns}
                connectionPatterns={timelineConnectionPatterns}
                class="w-full h-full"
                asBanner={true}
                bannerHeight="100%"
              />
            </div>
          </div>
        </div>
      </div>
    )}

    <!-- IMAGE BANNER -->
    {hasImageBanner && (
      <div class="banner-container-image">
        <div class="banner-aspect-container">
          <div class="banner-content-wrapper">
            {finalBannerLink ? (
              <a 
                href={finalBannerLink} 
                aria-label="Banner Link" 
                class="group block w-full h-full"
              >
                <div class="absolute transition pointer-events-none group-hover:bg-black/30 group-active:bg-black/50 w-full h-full z-50 flex items-center justify-center">
                  <Icon
                    name="fa6-solid:link"
                    class="text-white drop-shadow-lg"
                    style="font-size: clamp(2rem, 4vw, 4rem);"
                  ></Icon>
                </div>
                <ImageWrapper
                  src={hasPostBanner && post?.data?.bannerType === "image"
                    ? normalizeAssetUrl(post.data.bannerData?.imageUrl || post.data.image)
                    : normalizeAssetUrl(bannerDataSources.imageBannerData?.imageUrl || siteConfig.banner.src)}
                  alt={hasPostBanner ? (post.data.title || "Post banner image") : (title || "Banner image")}
                  class="w-full h-full object-cover"
                  imgClass="object-cover"
                  loading="eager"
                />
              </a>
            ) : (
              <ImageWrapper
                src={hasPostBanner && post?.data?.bannerType === "image"
                  ? normalizeAssetUrl(post.data.bannerData?.imageUrl || post.data.image)
                  : normalizeAssetUrl(bannerDataSources.imageBannerData?.imageUrl || siteConfig.banner.src)}
                alt={hasPostBanner ? (post.data.title || "Post banner image") : (title || "Banner image")}
                class="w-full h-full object-cover"
                imgClass="object-cover"
                loading="eager"
              />
            )}
          </div>
        </div>
      </div>
    )}

    <!-- STANDARD BANNER -->
    {hasStandardBanner && (
      <div class="banner-container-standard">
        <div class="banner-aspect-container">
          <div class="banner-content-wrapper">
            <div id="standard-banner-container">
	              {bannerConfig.standardBannerConfig.bannerList.length > 0 ? bannerConfig.standardBannerConfig.bannerList.map((item, index) => {
	                const bannerLink = getBannerLink(index);
	                const previewDetails = getBannerItemPreviewDetails(index);
	                const isClickable = bannerLink !== null;
	                const isVideoItem = bannerConfig.standardBannerConfig.isVideoBannerItem(item);
	                const isImageItem = bannerConfig.standardBannerConfig.isImageBannerItem(item);
                  const isInitialSlide = index === 0;
	                
	                return (
	                  <div
	                    class="banner-slide absolute inset-0 w-full h-full transition-opacity"
	                    style={`
	                      transition-duration: ${bannerConfig.standardBannerConfig.animation.transitionDuration}ms;
	                      opacity: ${index === 0 ? '1' : '0'};
	                      pointer-events: ${index === 0 ? '' : 'none'};
	                    `}
	                    data-banner-index={index}
	                    data-active={index === 0 ? 'true' : 'false'}
	                  >
                    <div
                      class="banner-link group w-full h-full relative cursor-pointer"
                      data-has-link={isClickable}
                      data-href={bannerLink}
                      aria-label={`Banner ${index + 1}${isClickable ? ' - Click to visit link' : ' - Click for information'}`}
                      role="button"
                      tabindex="0"
                    >
                      <div class="absolute inset-0 w-full h-full transition-all duration-300 opacity-0 group-hover:opacity-30 bg-black pointer-events-none z-10"></div>
                      
	                      <div
	                        class="banner-media-layer w-full h-full overflow-hidden"
	                        data-banner-media-type={isVideoItem ? 'video' : isImageItem ? 'image' : 'unknown'}
	                      >
                        {isVideoItem ? (
                          <>
                            <video
                              src={isInitialSlide ? item.src : undefined}
                              data-src={isInitialSlide ? undefined : item.src}
                              data-playback-rate={typeof item.playbackRate === 'number' ? item.playbackRate : undefined}
                              autoplay={bannerConfig.standardBannerConfig.video.autoplay}
                              muted={bannerConfig.standardBannerConfig.video.muted}
                              loop={bannerConfig.standardBannerConfig.video.loop}
                              playsinline={bannerConfig.standardBannerConfig.video.playsInLine}
                              controls={bannerConfig.standardBannerConfig.video.controls}
                              preload={isInitialSlide ? (item.preload || bannerConfig.standardBannerConfig.video.preload) : 'none'}
                              poster={item.fallbackImage}
                              class={`banner-video w-full h-full object-${bannerConfig.visual.objectFit} object-${bannerConfig.visual.objectPosition} transition-transform duration-300 group-hover:scale-105`}
                              style={`border-radius: ${bannerConfig.visual.borderRadius};`}
                              onerror="this.style.display='none'; var ne = this.nextElementSibling; if (ne) ne.style.display='block';"
                            >
                              Your browser does not support the video tag.
                            </video>
                            <Image
                              src={item.fallbackImage}
                              alt={item.alt}
                              width={bannerConfig.layout.maxWidth}
                              height={Math.round(bannerConfig.layout.maxWidth * 0.5625)}
                              class={`banner-image-fallback w-full h-full object-${bannerConfig.visual.objectFit} object-${bannerConfig.visual.objectPosition} transition-transform duration-300 group-hover:scale-105`}
                              style={`border-radius: ${bannerConfig.visual.borderRadius}; display: none;`}
                              loading={isInitialSlide ? 'eager' : 'lazy'}
                            />
                          </>
                        ) : isImageItem ? (
                          <Image
                            src={item.src}
                            alt={item.alt}
                            width={bannerConfig.layout.maxWidth}
                            height={Math.round(bannerConfig.layout.maxWidth * 0.5625)}
                            class={`banner-image w-full h-full object-${bannerConfig.visual.objectFit} object-${bannerConfig.visual.objectPosition} transition-transform duration-300 group-hover:scale-105`}
                            style={`border-radius: ${bannerConfig.visual.borderRadius};`}
                            loading={isInitialSlide ? 'eager' : 'lazy'}
                          />
                        ) : (
	                          <slot name="banner-slide-content" />
	                        )}
	                      </div>
	                      {previewDetails && (
	                        <div class="banner-title-display">
	                          <div class="banner-title-kicker">
	                            {previewDetails.hasValidLink ? 'Destination' : 'Viewscreen'}
	                          </div>
	                          <h3>{previewDetails.previewTitle}</h3>
	                          <p>{previewDetails.previewDescription}</p>
	                        </div>
	                      )}
	                    </div>
	                  </div>
	                );
	              }) : (
                <div class="w-full h-full rounded-lg" style={`background: ${getFallbackBannerCSS()};`}></div>
              )}
              {shouldShowMobilePortraitRightRailHero && (
                <div id="mobile-banner-right-rail-hero" class="hidden">
                  <div class="mobile-banner-right-rail-shell">
                    <RightRailContent
                      currentPath={currentPath}
                      post={post}
                      isHomePage={isHomePage}
                    />
                  </div>
                </div>
              )}
              
              {bannerConfig.visual.applyGradientOverlay && (
                <div class="absolute inset-0 w-full h-full pointer-events-none" style={`background: ${bannerConfig.visual.gradientOverlay}; border-radius: ${bannerConfig.visual.borderRadius};`}></div>
              )}
            </div>
          </div>
        </div>
      </div>
    )}
  </div>

  <!-- =============================================================== -->
  <!-- ⭐ SIMPLIFIED: One column mobile, two column desktop           -->
  <!-- =============================================================== -->
  <div class={`w-full z-30 pointer-events-none relative`}>
    <div id="main-panel-wrapper" class="responsive-panel-wrapper relative max-w-[var(--page-width)] mx-auto pointer-events-auto" data-is-home={isHomePage ? "true" : "false"} data-home-layout={isPortalHome ? 'portal' : 'default'}>
      <div
        id="main-grid"
        class:list={[gridLayoutClasses, 'transition-swup-fade', { 'has-right-rail': shouldShowRightRail }]}
        style={`margin-top: ${mainContentOffset};`}
        data-mobile-right-rail-hero={shouldShowMobilePortraitRightRailHero ? mobilePortraitRightRailHeroStyle : 'default'}
      >

        <!-- Grid Item 1: SIDEBAR -->
        {!isPortalHome && !isBannerOnly && (
        <div class="hidden md:block mb-4 col-span-1 md:max-w-[17.5rem] onload-animation mobile-support-column">
          <section class="mobile-support-group mobile-support-profile-group">
            <div class="mobile-support-label">About</div>
            <div class="mb-4">
            <Profile client:load slug={processedAuthorData.slug} customAvatar={processedAuthorData.customAvatar} customName={processedAuthorData.customName} customBio={processedAuthorData.customBio} customLink={processedAuthorData.customLink} isHomePage={isHomePage} profileConfig={profileConfig} avatarConfig={avatarConfig} />
            </div>
          </section>
          {shouldShowToc && (
            <section id="sidebar-mobile-toc" class="mobile-support-group mobile-support-toc-group mb-4 block md:hidden">
              <div class="mobile-support-label">On this page</div>
              <div class="card-base overflow-hidden">
                <div class="font-bold transition text-lg text-neutral-900 dark:text-neutral-100 relative ml-8 mt-4 mb-2 before:w-1 before:h-4 before:rounded-md before:bg-[var(--primary)] before:absolute before:left-[-16px] before:top-[5.5px]">
                  Contents
                </div>
                <div class="px-4 pb-4">
                  <TOC headings={headings}></TOC>
                </div>
              </div>
            </section>
          )}
          <SideBar class="flex-1" headings={headings} postSlug={processedAuthorData.slug} customAvatar={processedAuthorData.customAvatar} customName={processedAuthorData.customName} customBio={processedAuthorData.customBio} pageMascotContext={Astro.props.pageMascotContext} hideProfile={true} showAssistantWidget={showAssistantWidget}>
            {shouldMergeRightRailIntoSidebar && (
              <div
                id="sidebar-right-rail-merge"
                class="hidden md:flex 2xl:hidden flex-col gap-4 pt-2"
              >
                <RightRailContent
                  currentPath={currentPath}
                  post={post}
                  isHomePage={isHomePage}
                />
              </div>
              )}
          </SideBar>
        </div>
        )}

        <!-- Grid Item 2: MAIN CONTENT -->
        <main id="main" class="col-span-1 overflow-hidden">
          <div id="main-content-wrapper" class="onload-animation">
            <div
              id="author-context"
              hidden
              aria-hidden="true"
              data-slug={processedAuthorData.slug}
              data-custom-avatar={processedAuthorData.customAvatar}
              data-custom-name={processedAuthorData.customName}
              data-custom-bio={processedAuthorData.customBio}
              data-custom-link={processedAuthorData.customLink}
              data-is-home-page={isHomePage}
            ></div>
            {!isBannerOnly && <slot />}
            <div class="footer onload-animation hidden md:block">
              <Footer></Footer>
            </div>
          </div>
        </main>

        <!-- Grid Item 3: RIGHT RAIL -->
        {shouldShowRightRail && !isBannerOnly && (
          <div
            id="toc-wrapper"
            class="hidden 2xl:block z-0"
            data-rail-mode={shouldShowToc ? 'toc' : 'widget'}
          >
            {shouldShowToc ? (
              <div
                class="sticky"
                style={`top: ${rightRailStickyTop};`}
              >
                <div class="card-base overflow-hidden onload-animation">
                  <div class="font-bold transition text-lg text-neutral-900 dark:text-neutral-100 relative ml-8 mt-4 mb-2 before:w-1 before:h-4 before:rounded-md before:bg-[var(--primary)] before:absolute before:left-[-16px] before:top-[5.5px]">
                    Contents
                  </div>
                  <div
                    id="toc-inner-wrapper"
                    class="max-h-[calc(100vh_-_12rem)] overflow-y-auto hide-scrollbar px-4 pb-4"
                    style="mask-image: none;"
                  >
                    <TOC headings={headings}></TOC>
                  </div>
                </div>
              </div>
            ) : (
              <div
                id="toc-inner-wrapper"
                class="transition-all duration-300"
                style="mask-image: none;"
              >
                <RightRailContent
                  currentPath={currentPath}
                  post={post}
                  isHomePage={isHomePage}
                />
              </div>
            )}
          </div>
        )}

        <!-- Mobile-only Footer -->
        <div class="footer col-span-1 onload-animation block md:hidden 2xl:hidden">
          <Footer></Footer>
        </div>
      </div>

      <BackToTop></BackToTop>
    </div>
  </div>


  <!-- =============================================================== -->
  <!-- APP MASCOT SLOT (e.g. Bleepy for MEGAMEAL)                    -->
  <!-- =============================================================== -->
  {contextForBleepy && (
    <div id="bleepy-page-context" style="display: none;" data-context={JSON.stringify(contextForBleepy)}></div>
  )}
  <slot name="mascot" />
  <slot name="overlay" />

  <!-- =============================================================== -->
  <!-- ⭐ NEW: CLIENT-SIDE SVELTE COMPONENTS                          -->
  <!-- =============================================================== -->
  
  <!-- Sidenav panels with settings and navigation -->
  <SidenavPanels client:idle siteConfig={siteConfig} />
  
  <!-- ⭐ UPDATED: Mobile profile overlay with custom author data -->
  <MobileProfileOverlay 
    client:idle 
    postSlug={processedAuthorData.slug}
    customAvatar={processedAuthorData.customAvatar}
    customName={processedAuthorData.customName}
    customBio={processedAuthorData.customBio}
    customLink={processedAuthorData.customLink}
    profileConfig={profileConfig}
    avatarConfig={avatarConfig}
  />
  
  <!-- ⭐ UPDATED: Special page features with oneColumn support -->
  <SpecialPageFeatures 
    client:idle 
    currentPath={currentPath} 
    oneColumn={oneColumnMode}
  />
  
  <!-- Scroll effects and parallax -->
  <ScrollEffects client:idle bannerConfig={bannerConfig} siteConfig={siteConfig} />
  
  <!-- Page load animations -->
  <PageAnimations client:load />

</Layout>

<!-- =============================================================== -->
<!-- ⭐ SUPER SIMPLIFIED: CSS with only essential positioning       -->
<!-- =============================================================== -->
<style define:vars={{
  'main-content-offset': mainContentOffset,
  'main-panel-top': mainPanelTop,
  'mobile-main-panel-top': mobileMainPanelTop,
  'navbar-spacing': navbarSpacing,
  'mobile-navbar-spacing':
    bannerConfig.navbar.mobileBannerGap ??
    bannerConfig.navbar.mobilePortraitSpacing,
  'banner-height': resolvedBannerHeight,
  'banner-height-mobile': resolvedBannerHeightMobile,
}}>
  /* ⭐ CRITICAL: Explicit mobile touch scrolling enablement */
  html, body {
    touch-action: pan-y pan-x !important;
    overscroll-behavior: auto !important;
    -webkit-overflow-scrolling: touch !important;
  }
  
  /* ⭐ CRITICAL: Ensure main content areas allow scrolling on mobile */
  @media (max-width: 767px) {
    body, html, #main, #main-content-wrapper, #main-grid, #main-panel-wrapper {
      touch-action: pan-y pan-x !important;
      overscroll-behavior: auto !important;
      -webkit-overflow-scrolling: touch !important;
    }
  }
  
  /* ⭐ ESSENTIAL: Main grid margin */
  #main-grid {
    margin-top: var(--main-content-offset);
  }

  .mobile-support-label,
  :global(#sidebar .sidebar-group-label) {
    display: none;
  }
  
  /* ⭐ ESSENTIAL: Main panel positioning */
  .responsive-panel-wrapper {
    top: var(--main-panel-top);
    transition: top 0.3s ease;
  }
  
  /* ⭐ ESSENTIAL: Banner spacing from navbar */
  #banner-container {
    margin-top: var(--navbar-spacing);
    transition: margin-top 0.3s ease;
  }

  /* Shared source of truth for banner height */
  #banner-container .banner-aspect-container {
    height: var(--banner-height);
  }

  #banner-container.banner-only-mode.has-timeline-banner .banner-container-timeline,
  #banner-container.banner-only-mode.has-timeline-banner .banner-content-wrapper,
  #banner-container.banner-only-mode.has-timeline-banner #timeline-mobile-wrapper {
    height: 100%;
  }

  #banner-container.banner-only-mode.has-timeline-banner .banner-aspect-container {
    height: var(--banner-height);
  }

  #banner-container.banner-only-mode.has-timeline-banner .banner-content-wrapper--timeline-hero {
    overflow: hidden;
    border-radius: 1.5rem;
  }

  /* Disable legacy aspect-ratio sizing; height comes from config via MainGridLayout */
  #banner-container .banner-aspect-container::before {
    padding-bottom: 0 !important;
  }
  
  /* ⭐ SIMPLIFIED: Mobile spacing (navbar always visible now) */
  @media (max-width: 767px) {
    .responsive-panel-wrapper {
      top: var(--mobile-main-panel-top);
    }

    #banner-container {
      margin-top: var(--mobile-navbar-spacing);
    }

    #banner-container .banner-aspect-container {
      height: var(--banner-height-mobile, var(--banner-height));
    }
  }

  @media (max-width: 767px) and (orientation: landscape) {
    #main-grid {
      grid-template-columns: 10rem minmax(0, 1fr) !important;
      gap: 0.75rem !important;
      padding-left: 0.5rem !important;
      padding-right: 0.5rem !important;
    }

    #main-grid > div:first-child {
      display: block !important;
      grid-column: 1 !important;
      max-width: 10rem !important;
      margin-bottom: 0 !important;
    }

    main#main {
      grid-column: 2 !important;
    }

    #sidebar-right-rail-merge {
      display: flex !important;
    }
  }

  @media (max-width: 767px) and (orientation: portrait) {
    #banner-container[data-mobile-right-rail-hero='banner-overlay'] .banner-aspect-container {
      height: max(34rem, var(--banner-height-mobile, var(--banner-height))) !important;
    }

    #banner-container[data-mobile-right-rail-hero='banner-overlay'] #mobile-banner-right-rail-hero {
      display: block !important;
      position: absolute;
      inset: 0;
      z-index: 40;
      display: flex !important;
      align-items: center;
      padding: 1.25rem 1rem;
      pointer-events: none;
    }

    #banner-container[data-mobile-right-rail-hero='banner-overlay'] #mobile-banner-right-rail-hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, rgb(15 23 42 / 0.2), rgb(15 23 42 / 0.56));
      pointer-events: none;
    }

    #banner-container[data-mobile-right-rail-hero='banner-overlay'] .mobile-banner-right-rail-shell {
      position: relative;
      overflow: visible;
      padding: 0;
      max-width: 40rem;
      margin: 0 auto;
      pointer-events: auto;
    }

    #banner-container[data-mobile-right-rail-hero='banner-overlay'] .mobile-banner-right-rail-shell > * {
      position: relative;
      z-index: 1;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-rail-shell) {
      gap: 0 !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-rail-widget) {
      background-color: transparent !important;
      background: transparent !important;
      border: 0 !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
      padding-bottom: 0 !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-rail-widget.card-base),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .widget-layout-card),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-journal-link.btn-card) {
      background-color: transparent !important;
      background: transparent !important;
      border: 0 !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .widget-layout-body) {
      padding: 0 !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-summary-card) {
      gap: 0.8rem;
      padding: 0;
      background-color: transparent !important;
      background: transparent;
      border: 0 !important;
      box-shadow: none !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-summary-status) {
      gap: 0.2rem;
      padding-bottom: 0.55rem;
      border-bottom-color: rgb(255 255 255 / 0.18);
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-summary-grid) {
      gap: 0.45rem;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-summary-row) {
      gap: 0.3rem 0.75rem;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-label),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-link-kicker) {
      color: rgb(255 255 255 / 0.7) !important;
      text-shadow:
        0 1px 2px rgb(15 23 42 / 0.8),
        0 3px 12px rgb(15 23 42 / 0.55);
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-value),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-preview-title),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-link-label) {
      color: rgb(255 255 255 / 0.96) !important;
      text-shadow:
        0 1px 2px rgb(15 23 42 / 0.85),
        0 4px 18px rgb(15 23 42 / 0.65);
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-value) {
      font-size: 1rem !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-preview-title) {
      font-size: 1.08rem !important;
      line-height: 1.28 !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-preview-date),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-preview-meta),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-meta) {
      color: rgb(255 255 255 / 0.62) !important;
      text-shadow:
        0 1px 2px rgb(15 23 42 / 0.72),
        0 3px 10px rgb(15 23 42 / 0.5);
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-preview-copy),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-note-copy) {
      color: rgb(255 255 255 / 0.84) !important;
      text-shadow:
        0 1px 2px rgb(15 23 42 / 0.78),
        0 4px 14px rgb(15 23 42 / 0.55);
      display: -webkit-box;
      overflow: hidden;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 3;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-intro-copy) {
      display: none !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-journal-link) {
      margin-top: 0.35rem !important;
      padding: 0 !important;
      justify-content: flex-start !important;
      gap: 0.5rem !important;
      background-color: transparent !important;
      background: transparent !important;
      border: 0 !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
      border-radius: 0 !important;
      min-height: 0 !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-journal-link:hover),
    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-journal-link:active) {
      background-color: transparent !important;
      background: transparent !important;
    }

    :global(#banner-container[data-mobile-right-rail-hero='banner-overlay'] .updates-journal-link > div:last-child) {
      color: rgb(255 255 255 / 0.88) !important;
      text-shadow:
        0 1px 2px rgb(15 23 42 / 0.78),
        0 4px 14px rgb(15 23 42 / 0.55);
    }

    #main-grid {
      grid-template-columns: minmax(0, 1fr) !important;
      gap: 1rem !important;
      padding-left: 0.75rem !important;
      padding-right: 0.75rem !important;
    }

    #main-grid > div:first-child {
      display: block !important;
      grid-column: 1 !important;
      max-width: none !important;
      margin-bottom: 0 !important;
      order: 2 !important;
      padding: 1rem !important;
      border-radius: 1.5rem !important;
      background:
        linear-gradient(180deg, rgb(0 0 0 / 0.035), rgb(0 0 0 / 0.02)) !important;
      border: 1px solid rgb(0 0 0 / 0.08) !important;
    }

    main#main {
      grid-column: 1 !important;
      order: 1 !important;
    }

    .mobile-support-label,
    #sidebar .sidebar-group-label {
      display: block !important;
      font-size: 0.72rem !important;
      font-weight: 700 !important;
      letter-spacing: 0.12em !important;
      text-transform: uppercase !important;
      color: rgb(0 0 0 / 0.45) !important;
      margin-bottom: 0.7rem !important;
    }

    #main-grid > div:last-child {
      order: 3 !important;
    }

    #sidebar {
      max-width: none !important;
    }

    #sidebar-sticky {
      position: static !important;
      top: auto !important;
      gap: 1.25rem !important;
    }

    #sidebar-right-rail-merge {
      display: flex !important;
      padding-top: 0 !important;
    }

    #main-grid[data-mobile-right-rail-hero='banner-overlay'] #sidebar-right-rail-merge {
      display: none !important;
    }

    .mobile-support-column {
      display: flex !important;
      flex-direction: column !important;
      gap: 1.25rem !important;
    }

    .mobile-support-group,
    #sidebar .sidebar-support-group {
      position: relative;
    }

    .mobile-support-profile-group {
      padding-bottom: 0.1rem;
    }

    .mobile-support-toc-group {
      margin-bottom: 0 !important;
      padding-top: 0.1rem;
      border-top: 1px solid rgb(0 0 0 / 0.08);
    }

    #sidebar .sidebar-support-group {
      padding-top: 1rem;
      border-top: 1px solid rgb(0 0 0 / 0.08);
    }

    #sidebar .sidebar-support-group:first-child {
      padding-top: 0;
      border-top: 0;
    }

    #sidebar .sidebar-slot-section {
      order: 1;
    }

    #sidebar .sidebar-categories-section {
      order: 2;
    }

    #sidebar .sidebar-tags-section {
      order: 3;
    }

    #main-grid > div:first-child .card-base,
    #main-grid > div:first-child :global(#bleepy-post-widget),
    #main-grid > div:first-child :global(.facts-widget-shell) {
      box-shadow: none !important;
      backdrop-filter: none !important;
    }

    #main-grid > div:first-child .sidebar-profile-card,
    #main-grid > div:first-child .widget-layout-card,
    #main-grid > div:first-child #sidebar-mobile-toc .card-base,
    #main-grid > div:first-child :global(#bleepy-post-widget),
    #main-grid > div:first-child :global(.facts-widget-shell) {
      background: rgb(255 255 255 / 0.5) !important;
      border: 1px solid rgb(0 0 0 / 0.06) !important;
    }

    #main-grid > div:first-child .widget-layout-title {
      margin-bottom: 0.65rem !important;
    }

    #main-grid > div:first-child .widget-layout-card {
      padding-bottom: 0.9rem !important;
    }

    :global(main#main .local-post.postcard-shell) {
      display: flex !important;
      flex-direction: column !important;
      height: auto !important;
    }

    :global(main#main .local-post .postcard-cover-link) {
      width: auto !important;
      aspect-ratio: 16 / 9;
      margin: 0.75rem 0.75rem 0 !important;
      border-radius: 1rem !important;
    }

    :global(main#main .local-post .postcard-mobile-meta) {
      display: flex !important;
      padding: 0.75rem 1rem 0 !important;
      margin-bottom: 0 !important;
    }

    :global(main#main .local-post .postcard-content) {
      width: 100% !important;
      padding: 0.55rem 1rem 1rem !important;
    }

    :global(main#main .local-post .postcard-title-link) {
      min-height: 0 !important;
      margin-bottom: 0.45rem !important;
      padding-right: 0 !important;
      font-size: 1.15rem !important;
      line-height: 1.25 !important;
      -webkit-line-clamp: 3;
    }

    :global(main#main .local-post .postcard-mobile-chevron) {
      display: none !important;
    }

    :global(main#main .local-post .postcard-desktop-meta),
    :global(main#main .local-post .postcard-reading-meta),
    :global(main#main .local-post .postcard-mobile-date-badge),
    :global(main#main .local-post .postcard-no-cover-date) {
      display: none !important;
    }

    :global(main#main .local-post .postcard-description) {
      min-height: 0 !important;
      margin-bottom: 0 !important;
      font-size: 0.92rem !important;
      line-height: 1.45 !important;
      -webkit-line-clamp: 3;
    }
  }

  @media (max-width: 767px) and (orientation: portrait) {
    :global(.dark) #main-grid > div:first-child {
      background:
        linear-gradient(180deg, rgb(255 255 255 / 0.05), rgb(255 255 255 / 0.03)) !important;
      border-color: rgb(255 255 255 / 0.1) !important;
    }

    :global(.dark) .mobile-support-label,
    :global(.dark) #sidebar .sidebar-group-label {
      color: rgb(255 255 255 / 0.48) !important;
    }

    :global(.dark) .mobile-support-toc-group,
    :global(.dark) #sidebar .sidebar-support-group {
      border-top-color: rgb(255 255 255 / 0.1) !important;
    }

    :global(.dark) #main-grid > div:first-child .sidebar-profile-card,
    :global(.dark) #main-grid > div:first-child .widget-layout-card,
    :global(.dark) #main-grid > div:first-child #sidebar-mobile-toc .card-base,
    :global(.dark) #main-grid > div:first-child #bleepy-post-widget,
    :global(.dark) #main-grid > div:first-child .facts-widget-shell {
      background: rgb(255 255 255 / 0.04) !important;
      border-color: rgb(255 255 255 / 0.08) !important;
    }
  }

  :global(#sidebar) {
    container-type: inline-size;
    container-name: sidebar-rail;
    --rail-card-padding: 0.75rem;
    --rail-card-padding-inline: 1rem;
    --rail-card-stack-gap: 1rem;
    --rail-widget-title-size: 1.125rem;
    --rail-copy-size: 0.95rem;
    --rail-copy-leading: 1.55;
    --rail-muted-size: 0.85rem;
    --rail-kicker-size: 0.72rem;
    --rail-button-height: 2.5rem;
    --rail-button-font-size: 0.95rem;
    --rail-tag-height: 2rem;
    --rail-badge-height: 1.75rem;
    --rail-badge-font-size: 0.875rem;
    --rail-avatar-max-width: none;
    --rail-profile-name-size: 1.25rem;
    --rail-profile-bio-size: 0.95rem;
    --rail-social-button-size: 2.5rem;
    --rail-social-icon-size: 1.5rem;
    --rail-facts-min-height: 24rem;
    --rail-chat-height: 16rem;
    --rail-chat-mascot-size: 12rem;
  }

  @container sidebar-rail (max-width: 13rem) {
    :global(#sidebar) {
      --rail-card-padding: 0.6rem;
      --rail-card-padding-inline: 0.72rem;
      --rail-card-stack-gap: 0.72rem;
      --rail-widget-title-size: 0.95rem;
      --rail-copy-size: 0.82rem;
      --rail-copy-leading: 1.4;
      --rail-muted-size: 0.74rem;
      --rail-kicker-size: 0.64rem;
      --rail-button-height: 2.05rem;
      --rail-button-font-size: 0.78rem;
      --rail-tag-height: 1.7rem;
      --rail-badge-height: 1.35rem;
      --rail-badge-font-size: 0.72rem;
      --rail-avatar-max-width: 7.4rem;
      --rail-profile-name-size: 0.94rem;
      --rail-profile-bio-size: 0.78rem;
      --rail-social-button-size: 1.95rem;
      --rail-social-icon-size: 1.05rem;
      --rail-facts-min-height: 18rem;
      --rail-chat-height: 11rem;
      --rail-chat-mascot-size: 6.4rem;
    }
  }

  @container sidebar-rail (max-width: 10.5rem) {
    :global(#sidebar) {
      --rail-card-padding: 0.48rem;
      --rail-card-padding-inline: 0.58rem;
      --rail-card-stack-gap: 0.6rem;
      --rail-widget-title-size: 0.86rem;
      --rail-copy-size: 0.75rem;
      --rail-copy-leading: 1.34;
      --rail-muted-size: 0.68rem;
      --rail-kicker-size: 0.58rem;
      --rail-button-height: 1.85rem;
      --rail-button-font-size: 0.72rem;
      --rail-tag-height: 1.5rem;
      --rail-badge-height: 1.2rem;
      --rail-badge-font-size: 0.66rem;
      --rail-avatar-max-width: 6rem;
      --rail-profile-name-size: 0.86rem;
      --rail-profile-bio-size: 0.72rem;
      --rail-social-button-size: 1.7rem;
      --rail-social-icon-size: 0.9rem;
      --rail-facts-min-height: 15.5rem;
      --rail-chat-height: 9rem;
      --rail-chat-mascot-size: 4.85rem;
    }
  }

  :global(#sidebar .sidebar-profile-card) {
    padding: var(--rail-card-padding) !important;
  }

  :global(#sidebar .sidebar-profile-avatar-link) {
    max-width: var(--rail-avatar-max-width) !important;
    margin-bottom: var(--rail-card-stack-gap) !important;
  }

  :global(#sidebar .sidebar-profile-name) {
    font-size: var(--rail-profile-name-size) !important;
    line-height: 1.2 !important;
  }

  :global(#sidebar .sidebar-profile-bio) {
    font-size: var(--rail-profile-bio-size) !important;
    line-height: 1.4 !important;
    margin-bottom: var(--rail-card-stack-gap) !important;
  }

  :global(#sidebar .sidebar-profile-socials) {
    gap: clamp(0.35rem, 1.5cqi, 0.5rem) !important;
  }

  :global(#sidebar .sidebar-profile-social) {
    width: var(--rail-social-button-size) !important;
    height: var(--rail-social-button-size) !important;
  }

  :global(#sidebar .sidebar-profile-social-wide) {
    width: auto !important;
    padding-inline: clamp(0.55rem, 3cqi, 0.75rem) !important;
    font-size: var(--rail-button-font-size) !important;
  }

  :global(#sidebar .sidebar-profile-social-icon) {
    font-size: var(--rail-social-icon-size) !important;
  }

  :global(#sidebar .widget-layout-card) {
    padding-bottom: var(--rail-card-stack-gap) !important;
  }

  :global(#sidebar .widget-layout-title) {
    font-size: var(--rail-widget-title-size) !important;
    line-height: 1.25 !important;
    margin-top: calc(var(--rail-card-padding) + 0.25rem) !important;
    margin-bottom: 0.5rem !important;
  }

  :global(#sidebar .widget-layout-body),
  :global(#sidebar .widget-layout-expand) {
    padding-inline: var(--rail-card-padding-inline) !important;
  }

  :global(#sidebar .widget-layout-more-button) {
    min-height: calc(var(--rail-button-height) - 0.15rem) !important;
    height: auto !important;
  }

  :global(#sidebar .widget-layout-more-label) {
    font-size: var(--rail-button-font-size) !important;
  }

  :global(#sidebar .sidebar-link-button) {
    min-height: var(--rail-button-height) !important;
    height: auto !important;
    padding-block: 0.3rem !important;
    padding-left: 0.35rem !important;
    padding-right: 0.35rem !important;
  }

  :global(#sidebar .sidebar-link-button > div) {
    margin-right: 0 !important;
    gap: clamp(0.3rem, 1.8cqi, 0.5rem) !important;
  }

  :global(#sidebar .sidebar-link-label),
  :global(#sidebar .sidebar-tag-button) {
    font-size: var(--rail-button-font-size) !important;
    line-height: 1.25 !important;
  }

  :global(#sidebar .sidebar-link-badge) {
    height: var(--rail-badge-height) !important;
    min-width: var(--rail-badge-height) !important;
    font-size: var(--rail-badge-font-size) !important;
    margin-left: clamp(0.3rem, 1.8cqi, 0.45rem) !important;
    padding-inline: 0.35rem !important;
  }

  :global(#sidebar .sidebar-tag-button) {
    min-height: var(--rail-tag-height) !important;
    height: auto !important;
    padding-inline: 0.55rem !important;
  }

  :global(#sidebar .updates-summary-card) {
    padding: clamp(0.55rem, 3cqi, 0.9rem) !important;
    gap: clamp(0.5rem, 2cqi, 0.875rem) !important;
  }

  :global(#sidebar .updates-label),
  :global(#sidebar .updates-link-kicker),
  :global(#sidebar .facts-widget-kicker) {
    font-size: var(--rail-kicker-size) !important;
  }

  :global(#sidebar .updates-value),
  :global(#sidebar .updates-preview-title),
  :global(#sidebar .updates-link-label),
  :global(#sidebar .facts-widget-title) {
    font-size: var(--rail-copy-size) !important;
    line-height: 1.35 !important;
  }

  :global(#sidebar .updates-meta),
  :global(#sidebar .updates-preview-date),
  :global(#sidebar .updates-preview-meta),
  :global(#sidebar .updates-note-copy),
  :global(#sidebar .updates-preview-copy),
  :global(#sidebar .updates-intro-copy),
  :global(#sidebar .facts-widget-footer) {
    font-size: var(--rail-muted-size) !important;
    line-height: var(--rail-copy-leading) !important;
  }

  :global(#sidebar .updates-journal-link) {
    padding-inline: clamp(0.7rem, 3cqi, 1rem) !important;
    padding-block: clamp(0.65rem, 2cqi, 0.85rem) !important;
  }

  :global(#sidebar .facts-widget-container),
  :global(#sidebar .facts-widget-shell) {
    min-height: var(--rail-facts-min-height) !important;
  }

  :global(#sidebar .facts-widget-shell) {
    gap: var(--rail-card-stack-gap) !important;
    padding: clamp(0.7rem, 3cqi, 1rem) !important;
  }

  :global(#sidebar #bleepy-post-widget) {
    padding: var(--rail-card-padding) !important;
    gap: var(--rail-card-stack-gap) !important;
  }

  :global(#sidebar #bleepy-post-widget .chat-history) {
    height: var(--rail-chat-height) !important;
    padding: clamp(0.55rem, 2.5cqi, 0.75rem) !important;
  }

  :global(#sidebar #widget-mascot-visual-area) {
    width: var(--rail-chat-mascot-size) !important;
    height: var(--rail-chat-mascot-size) !important;
    margin-bottom: 0.75rem !important;
  }

  :global(#sidebar #bleepy-post-widget p),
  :global(#sidebar #bleepy-post-widget .speech-bubble-content),
  :global(#sidebar #bleepy-post-widget .chat-input),
  :global(#sidebar #bleepy-post-widget .chat-send-button) {
    font-size: var(--rail-button-font-size) !important;
    line-height: var(--rail-copy-leading) !important;
  }

  :global(#sidebar #widget-chat-send) {
    padding-inline: clamp(0.6rem, 2.5cqi, 1rem) !important;
    padding-block: 0.45rem !important;
  }

  /* Homepage only: keep a compact two-column layout on mobile landscape */
  @media (max-width: 1023px) and (orientation: landscape) and (max-height: 500px) {
    body.is-home #main-grid {
      grid-template-columns: 12rem minmax(0, 1fr) !important;
      gap: 0.75rem !important;
      padding-left: 0.5rem !important;
      padding-right: 0.5rem !important;
    }

    body.is-home #main-grid > div:first-child {
      display: block !important;
      grid-column: 1 !important;
      max-width: 12rem !important;
      margin-bottom: 0 !important;
    }

    body.is-home main#main {
      grid-column: 2 !important;
    }

    body.is-home #sidebar-right-rail-merge {
      display: flex !important;
    }

    body.is-home #main-content-wrapper > .footer.hidden.md\\:block {
      display: block !important;
    }

    body.is-home #main-grid > div:last-child {
      display: none !important;
    }
  }

  .banner-media-layer {
    position: relative;
    overflow: hidden;
  }

  .banner-media-layer > .banner-image,
  .banner-media-layer > .banner-video,
  .banner-media-layer > .banner-image-fallback {
    transform-origin: center center;
  }

  .banner-media-layer.banner-motion-active > .banner-image,
  .banner-media-layer.banner-motion-active > .banner-video,
  .banner-media-layer.banner-motion-active > .banner-image-fallback {
    animation-duration: var(--banner-motion-duration, 6000ms);
    animation-timing-function: var(--banner-motion-easing, ease-in-out);
    animation-fill-mode: both;
    animation-iteration-count: 1;
  }

  .banner-media-layer.banner-motion-zoom-in > .banner-image,
  .banner-media-layer.banner-motion-zoom-in > .banner-video,
  .banner-media-layer.banner-motion-zoom-in > .banner-image-fallback {
    animation-name: bannerMotionZoomIn;
  }

  .banner-media-layer.banner-motion-zoom-out > .banner-image,
  .banner-media-layer.banner-motion-zoom-out > .banner-video,
  .banner-media-layer.banner-motion-zoom-out > .banner-image-fallback {
    animation-name: bannerMotionZoomOut;
  }

  .banner-media-layer.banner-motion-pan-left > .banner-image,
  .banner-media-layer.banner-motion-pan-left > .banner-video,
  .banner-media-layer.banner-motion-pan-left > .banner-image-fallback {
    animation-name: bannerMotionPanLeft;
  }

  .banner-media-layer.banner-motion-pan-right > .banner-image,
  .banner-media-layer.banner-motion-pan-right > .banner-video,
  .banner-media-layer.banner-motion-pan-right > .banner-image-fallback {
    animation-name: bannerMotionPanRight;
  }

  .banner-media-layer.banner-motion-pan-up > .banner-image,
  .banner-media-layer.banner-motion-pan-up > .banner-video,
  .banner-media-layer.banner-motion-pan-up > .banner-image-fallback {
    animation-name: bannerMotionPanUp;
  }

  .banner-media-layer.banner-motion-pan-down > .banner-image,
  .banner-media-layer.banner-motion-pan-down > .banner-video,
  .banner-media-layer.banner-motion-pan-down > .banner-image-fallback {
    animation-name: bannerMotionPanDown;
  }

  @keyframes bannerMotionZoomIn {
    from {
      transform: scale(1);
    }
    to {
      transform: scale(var(--banner-motion-scale, 1.08));
    }
  }

  @keyframes bannerMotionZoomOut {
    from {
      transform: scale(var(--banner-motion-scale, 1.08));
    }
    to {
      transform: scale(1);
    }
  }

  @keyframes bannerMotionPanLeft {
    from {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, 0, 0);
    }
    to {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(calc(-1 * var(--banner-motion-pan-distance, 4%)), 0, 0);
    }
  }

  @keyframes bannerMotionPanRight {
    from {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(calc(-1 * var(--banner-motion-pan-distance, 4%)), 0, 0);
    }
    to {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, 0, 0);
    }
  }

  @keyframes bannerMotionPanUp {
    from {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, 0, 0);
    }
    to {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, calc(-1 * var(--banner-motion-pan-distance, 4%)), 0);
    }
  }

  @keyframes bannerMotionPanDown {
    from {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, calc(-1 * var(--banner-motion-pan-distance, 4%)), 0);
    }
    to {
      transform: scale(var(--banner-motion-pan-scale, 1.1))
        translate3d(0, 0, 0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .banner-media-layer.banner-motion-active > .banner-image,
    .banner-media-layer.banner-motion-active > .banner-video,
    .banner-media-layer.banner-motion-active > .banner-image-fallback {
      animation: none !important;
    }
  }
</style>

<!-- =============================================================== -->
<!-- ⭐ TOUCH EVENT DEBUG LOGGING SYSTEM                            -->
<!-- =============================================================== -->
<script is:inline define:vars={{ bannerAnimationSettings }}>
  // // ⭐ CRITICAL: Root cause debugging system
  // function initTouchDebugging() {
  //   console.log('🔍 Touch debugging initialized');
    
  //   let touchStartY = 0;
  //   let isTouchScrolling = false;
    
  //   // 🕵️ DETECTIVE MODE: Find what's blocking natural scrolling
  //   console.log('🕵️ Starting root cause investigation...');
    
  //   // Check CSS properties that might block scrolling
  //   const body = document.body;
  //   const html = document.documentElement;
    
  //   console.log('📋 Body CSS Analysis:', {
  //     overflow: getComputedStyle(body).overflow,
  //     overflowY: getComputedStyle(body).overflowY,
  //     touchAction: getComputedStyle(body).touchAction,
  //     position: getComputedStyle(body).position,
  //     height: getComputedStyle(body).height,
  //     pointerEvents: getComputedStyle(body).pointerEvents
  //   });
    
  //   console.log('📋 HTML CSS Analysis:', {
  //     overflow: getComputedStyle(html).overflow,
  //     overflowY: getComputedStyle(html).overflowY,
  //     touchAction: getComputedStyle(html).touchAction,
  //     height: getComputedStyle(html).height,
  //     pointerEvents: getComputedStyle(html).pointerEvents
  //   });
    
  //   // Test what happens when we try natural scrolling
  //   function testNaturalScrolling() {
  //     const originalScrollY = window.scrollY;
  //     console.log('🧪 Testing natural scroll from position:', originalScrollY);
      
  //     // Try programmatic scroll (this should work)
  //     window.scrollBy(0, 10);
  //     setTimeout(() => {
  //       const newScrollY = window.scrollY;
  //       console.log('🧪 Programmatic scroll result:', {
  //         from: originalScrollY,
  //         to: newScrollY,
  //         success: newScrollY !== originalScrollY
  //       });
        
  //       // Scroll back
  //       window.scrollTo(0, originalScrollY);
  //     }, 100);
  //   }
    
  //   testNaturalScrolling();
    
  //   // Force touch scrolling by manually handling touchmove
  //   document.addEventListener('touchstart', (e) => {
  //     touchStartY = e.touches[0].clientY;
  //     isTouchScrolling = false;
  //     console.log(`📱 touchstart:`, {
  //       type: e.type,
  //       touches: e.touches.length,
  //       startY: touchStartY,
  //       target: e.target.tagName + (e.target.id ? `#${e.target.id}` : '') + (e.target.className ? `.${e.target.className.split(' ')[0]}` : ''),
  //       defaultPrevented: e.defaultPrevented
  //     });
  //   }, { passive: true });
    
  //   document.addEventListener('touchmove', (e) => {
  //     const currentY = e.touches[0].clientY;
  //     const deltaY = touchStartY - currentY;
      
  //     console.log(`📱 touchmove:`, {
  //       type: e.type,
  //       deltaY: deltaY,
  //       currentScrollY: window.scrollY,
  //       target: e.target.tagName + (e.target.id ? `#${e.target.id}` : '') + (e.target.className ? `.${e.target.className.split(' ')[0]}` : ''),
  //       defaultPrevented: e.defaultPrevented
  //     });
      
      // FORCE SCROLLING: Manually scroll the window with better sensitivity
      // COMMENTED OUT FOR DEBUGGING - this was masking the real issue
      /*
      if (Math.abs(deltaY) > 2) { // Lower threshold for more responsive scrolling
        isTouchScrolling = true;
        // Multiply deltaY by 2 for more natural scrolling speed
        const scrollDelta = deltaY * 50;
        const newScrollY = Math.max(0, window.scrollY + scrollDelta);
        console.log(`🚀 FORCING SCROLL: from ${window.scrollY} to ${newScrollY} (delta: ${scrollDelta})`);
        window.scrollTo(0, newScrollY);
        touchStartY = currentY; // Update for next delta calculation
      }
      */
      
      // Check if preventDefault was called
  //     if (e.defaultPrevented) {
  //       console.warn(`⚠️ touchmove was prevented! Target:`, e.target);
  //     }
  //   }, { passive: true });
    
  //   document.addEventListener('touchend', (e) => {
  //     console.log(`📱 touchend:`, {
  //       type: e.type,
  //       wasScrolling: isTouchScrolling,
  //       finalScrollY: window.scrollY,
  //       defaultPrevented: e.defaultPrevented
  //     });
  //     isTouchScrolling = false;
  //   }, { passive: true });
    
  //   // Log scroll events
  //   let scrollTimeout;
  //   document.addEventListener('scroll', (e) => {
  //     clearTimeout(scrollTimeout);
  //     scrollTimeout = setTimeout(() => {
  //       console.log('📜 Scroll event:', {
  //         target: e.target === document ? 'document' : e.target.tagName,
  //         scrollY: window.scrollY,
  //         documentHeight: document.documentElement.scrollHeight,
  //         windowHeight: window.innerHeight
  //       });
  //     }, 100);
  //   }, { passive: true });
    
  //   // Log any event listeners that might interfere
  //   const originalAddEventListener = EventTarget.prototype.addEventListener;
  //   EventTarget.prototype.addEventListener = function(type, listener, options) {
  //     if (type.includes('touch') || type === 'scroll') {
  //       console.log(`🎯 Event listener added:`, {
  //         type,
  //         target: this.constructor.name,
  //         options,
  //         passive: options?.passive
  //       });
  //     }
  //     return originalAddEventListener.call(this, type, listener, options);
  //   };
  // }
  
  // Initialize immediately
  //initTouchDebugging();
  
  const bannerCleanupKey = '__merkinBannerCleanup';
  const bannerPageLoadBoundKey = '__merkinBannerPageLoadBound';

  const initializeBannerSystems = () => {
    if (typeof window === 'undefined') return;

    if (typeof window[bannerCleanupKey] === 'function') {
      window[bannerCleanupKey]();
    }

    const abortController = new AbortController();
    const { signal } = abortController;

    const standardBannerContainer = document.getElementById('standard-banner-container');
    const slides = Array.from(
      standardBannerContainer?.querySelectorAll('.banner-slide') || [],
    );
    const animationConfig = bannerAnimationSettings || {};
    const motionConfig = animationConfig.motion || {};
    const motionClassNames = [
      'banner-motion-active',
      'banner-motion-zoom-in',
      'banner-motion-zoom-out',
      'banner-motion-pan-left',
      'banner-motion-pan-right',
      'banner-motion-pan-up',
      'banner-motion-pan-down',
    ];
    const motionEffects = [
      'zoom-in',
      'zoom-out',
      'pan-left',
      'pan-right',
      'pan-up',
      'pan-down',
    ];
    const prefersReducedMotion =
      typeof window !== 'undefined' &&
      window.matchMedia &&
      window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const toFiniteNumber = (value, fallbackValue) => {
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : fallbackValue;
    };
    const interval = Math.max(1000, toFiniteNumber(animationConfig.interval, 5000));
    const transitionDuration = Math.max(
      0,
      toFiniteNumber(animationConfig.transitionDuration, 1000),
    );
    const randomStartEnabled = !!animationConfig.randomStart;
    const initialSlideIndex =
      randomStartEnabled && slides.length > 1
        ? Math.floor(Math.random() * slides.length)
        : 0;
    const motionEnabled = !!(
      animationConfig.enabled &&
      motionConfig.enabled &&
      !prefersReducedMotion
    );
    const motionDuration = Math.max(
      transitionDuration + 250,
      toFiniteNumber(motionConfig.duration, interval),
    );
    const motionScale = Math.min(
      1.3,
      Math.max(1.01, toFiniteNumber(motionConfig.scale, 1.08)),
    );
    const motionPanDistance = Math.min(
      12,
      Math.max(1, toFiniteNumber(motionConfig.panDistance, 4)),
    );
    const motionPanScale = Math.max(1.02, motionScale);
    const motionEasing =
      typeof motionConfig.easing === 'string' && motionConfig.easing.trim().length > 0
        ? motionConfig.easing.trim()
        : 'ease-in-out';
    const shouldAlternateDirection = motionConfig.alternate === true;
    const normalizeMotionMode = (modeValue) => {
      if (typeof modeValue !== 'string') return 'zoom-in';
      const normalized = modeValue.toLowerCase().replace(/[^a-z]/g, '');
      switch (normalized) {
        case 'zoomin':
        case 'pushin':
          return 'zoom-in';
        case 'zoomout':
        case 'pushout':
          return 'zoom-out';
        case 'panleft':
          return 'pan-left';
        case 'panright':
          return 'pan-right';
        case 'panup':
          return 'pan-up';
        case 'pandown':
          return 'pan-down';
        case 'pan':
        case 'panhorizontal':
          return 'pan-horizontal';
        case 'panvertical':
          return 'pan-vertical';
        case 'alternate':
        case 'mixed':
          return 'alternate';
        case 'random':
          return 'random';
        default:
          return 'zoom-in';
      }
    };
    const motionMode = normalizeMotionMode(motionConfig.mode);
    const oppositeEffect = {
      'zoom-in': 'zoom-out',
      'zoom-out': 'zoom-in',
      'pan-left': 'pan-right',
      'pan-right': 'pan-left',
      'pan-up': 'pan-down',
      'pan-down': 'pan-up',
    };
    let bannerIntervalId = null;
    let motionCleanupTimeoutId = null;
    let currentSlideIndex = initialSlideIndex;

    const clearMotionClasses = (mediaLayer) => {
      mediaLayer.classList.remove(...motionClassNames);
    };

    const applyVideoPlaybackRate = (videoElement) => {
      if (!videoElement) return;
      const configuredRate = Number(videoElement.dataset.playbackRate);
      videoElement.playbackRate =
        Number.isFinite(configuredRate) && configuredRate > 0 ? configuredRate : 1;
    };

    const ensureSlideMediaLoaded = (slideIndex) => {
      const slide = slides[slideIndex];
      if (!slide) return;

      slide
        .querySelectorAll('video[data-src]')
        .forEach((videoElement) => {
          const deferredSrc = videoElement.dataset.src;
          if (!deferredSrc || videoElement.getAttribute('src')) return;

          videoElement.src = deferredSrc;
          videoElement.removeAttribute('data-src');
          applyVideoPlaybackRate(videoElement);
          videoElement.load();
        });
    };

    const syncSlidePlayback = (activeIndex) => {
      slides.forEach((slide, index) => {
        const videoElement = slide.querySelector('video');
        if (!videoElement) return;
        applyVideoPlaybackRate(videoElement);

        if (index === activeIndex) {
          const playPromise = videoElement.play();
          if (playPromise && typeof playPromise.catch === 'function') {
            playPromise.catch(() => {});
          }
        } else {
          videoElement.pause();
        }
      });
    };

    const cleanupHiddenSlideMotion = () => {
      slides.forEach((slide, index) => {
        if (index === currentSlideIndex) return;
        const mediaLayer = slide.querySelector('.banner-media-layer');
        if (mediaLayer) {
          clearMotionClasses(mediaLayer);
        }
      });
    };

    const resolveMotionEffect = (slideIndex) => {
      if (motionMode === 'alternate') {
        return motionEffects[slideIndex % motionEffects.length];
      }
      if (motionMode === 'random') {
        return motionEffects[Math.floor(Math.random() * motionEffects.length)];
      }
      if (motionMode === 'pan-horizontal') {
        return slideIndex % 2 === 0 ? 'pan-left' : 'pan-right';
      }
      if (motionMode === 'pan-vertical') {
        return slideIndex % 2 === 0 ? 'pan-up' : 'pan-down';
      }
      if (shouldAlternateDirection && oppositeEffect[motionMode]) {
        return slideIndex % 2 === 0 ? motionMode : oppositeEffect[motionMode];
      }
      return motionMode;
    };

    const applyMotionToSlide = (slideIndex) => {
      if (!motionEnabled) return;
      const slide = slides[slideIndex];
      if (!slide) return;

      const mediaLayer = slide.querySelector('.banner-media-layer');
      if (!mediaLayer) return;

      const mediaType = mediaLayer.dataset.bannerMediaType;
      if (mediaType === 'unknown') return;

      const effect = resolveMotionEffect(slideIndex);

      clearMotionClasses(mediaLayer);
      mediaLayer.style.setProperty('--banner-motion-duration', `${motionDuration}ms`);
      mediaLayer.style.setProperty('--banner-motion-easing', motionEasing);
      mediaLayer.style.setProperty('--banner-motion-scale', String(motionScale));
      mediaLayer.style.setProperty('--banner-motion-pan-scale', String(motionPanScale));
      mediaLayer.style.setProperty(
        '--banner-motion-pan-distance',
        `${motionPanDistance}%`,
      );

      // Restart keyframes cleanly whenever a slide becomes active.
      void mediaLayer.offsetWidth;
      mediaLayer.classList.add('banner-motion-active', `banner-motion-${effect}`);
    };

    const showSlide = (nextIndex) => {
      if (!slides.length) return;

      ensureSlideMediaLoaded(nextIndex);

      slides.forEach((slide, index) => {
        const slideElement = slide;
        const isActive = index === nextIndex;
        slideElement.style.opacity = isActive ? '1' : '0';
        slideElement.style.pointerEvents = isActive ? '' : 'none';
        slideElement.dataset.active = isActive ? 'true' : 'false';
      });

      applyMotionToSlide(nextIndex);
      syncSlidePlayback(nextIndex);
      currentSlideIndex = nextIndex;

      if (motionCleanupTimeoutId) {
        clearTimeout(motionCleanupTimeoutId);
      }
      motionCleanupTimeoutId = setTimeout(() => {
        cleanupHiddenSlideMotion();
      }, Math.max(transitionDuration + 60, 120));
    };

    const startBannerAnimation = () => {
      if (!slides.length || slides.length <= 1 || !animationConfig.enabled) return;
      if (bannerIntervalId) clearInterval(bannerIntervalId);

      bannerIntervalId = setInterval(() => {
        const nextIndex = (currentSlideIndex + 1) % slides.length;
        showSlide(nextIndex);
      }, interval);
    };

    const stopBannerAnimation = () => {
      if (!bannerIntervalId) return;
      clearInterval(bannerIntervalId);
      bannerIntervalId = null;
    };

    if (standardBannerContainer && slides.length > 0) {
      showSlide(initialSlideIndex);
      startBannerAnimation();

      if (animationConfig.pauseOnHover !== false) {
        standardBannerContainer.addEventListener('mouseenter', stopBannerAnimation, {
          signal,
        });
        standardBannerContainer.addEventListener('mouseleave', startBannerAnimation, {
          signal,
        });
      }

      standardBannerContainer.querySelectorAll('.banner-link').forEach((linkElement) => {
        const hasLink = linkElement.dataset.hasLink === 'true';
        const href = linkElement.dataset.href;
        if (!hasLink || !href) return;

        linkElement.addEventListener('click', (e) => {
          // Don't override clicks on <a> links inside the slide (e.g., CTA buttons in html-type slides)
          if (e.target.closest('a[href]')) return;
          window.location.href = href;
        }, { signal });

        linkElement.addEventListener('keydown', (event) => {
          if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            window.location.href = href;
          }
        }, { signal });
      });

      document.addEventListener('visibilitychange', () => {
        if (document.hidden) {
          stopBannerAnimation();
        } else {
          startBannerAnimation();
        }
      }, { signal });
    }

    // ⭐ NEW: Timeline mobile interaction system
    const timelineWrapper = document.getElementById('timeline-mobile-wrapper');
    if (timelineWrapper && window.innerWidth < 768) {
      let isTimelineActive = false;

      // Handle timeline wrapper click/tap
      timelineWrapper.addEventListener('click', (e) => {
        if (!isTimelineActive) {
          // First tap - activate timeline
          e.preventDefault();
          e.stopPropagation();
          isTimelineActive = true;
          timelineWrapper.classList.remove('timeline-mobile-inactive');
          timelineWrapper.classList.add('timeline-mobile-active');
          if (window.location.hostname === 'localhost') {
            console.log('Timeline activated for mobile interaction');
          }
        }
        // If already active, let the click through to timeline controls
      }, { signal });

      // Reset timeline interaction when clicking outside
      document.addEventListener('click', (e) => {
        if (isTimelineActive && !timelineWrapper.contains(e.target)) {
          isTimelineActive = false;
          timelineWrapper.classList.remove('timeline-mobile-active');
          timelineWrapper.classList.add('timeline-mobile-inactive');
          if (window.location.hostname === 'localhost') {
            console.log('Timeline deactivated - clicked outside');
          }
        }
      }, { signal });

      // Reset timeline when navigating (blog post selection)
      window.addEventListener('beforeunload', () => {
        isTimelineActive = false;
        timelineWrapper.classList.remove('timeline-mobile-active');
        timelineWrapper.classList.add('timeline-mobile-inactive');
      }, { signal });

      // Reset on page transitions (for SPA behavior)
      document.addEventListener('astro:page-load', () => {
        isTimelineActive = false;
        const wrapper = document.getElementById('timeline-mobile-wrapper');
        if (wrapper) {
          wrapper.classList.remove('timeline-mobile-active');
          wrapper.classList.add('timeline-mobile-inactive');
        }
      }, { signal });
    }

    window[bannerCleanupKey] = () => {
      stopBannerAnimation();
      if (motionCleanupTimeoutId) {
        clearTimeout(motionCleanupTimeoutId);
        motionCleanupTimeoutId = null;
      }
      cleanupHiddenSlideMotion();
      abortController.abort();
    };
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeBannerSystems, { once: true });
  } else {
    initializeBannerSystems();
  }

  if (!window[bannerPageLoadBoundKey]) {
    document.addEventListener('astro:page-load', initializeBannerSystems);
    window[bannerPageLoadBoundKey] = true;
  }
</script>
