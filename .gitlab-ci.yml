# Minimal test - if this runs, runner infrastructure is working.
# We will restore the full deploy CI once this confirms working.

stages:
  - test

hello:
  stage: test
  script:
    - echo "GitLab CI runner is working"
    - node --version
    - npm --version
