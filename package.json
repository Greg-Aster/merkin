{
  "name": "merkin-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.14.4",
  "scripts": {
    "dev:travel": "pnpm --filter @merkin/travel dev",
    "dev:temporal": "pnpm --filter temporal-flow dev",
    "dev:dndiy": "pnpm --filter dndiy dev",
    "dev:megameal": "pnpm --filter @merkin/megameal dev",
    "build:all": "pnpm -r build",
    "build:travel": "pnpm --filter @merkin/travel build",
    "build:temporal": "pnpm --filter temporal-flow build",
    "build:dndiy": "pnpm --filter dndiy build",
    "build:megameal": "pnpm --filter @merkin/megameal build",
    "deploy:megameal": "pnpm build:megameal && find apps/megameal/dist -size +25M -delete && pnpm dlx wrangler pages deploy apps/megameal/dist --project-name merkin-megameal --branch ${DEPLOY_BRANCH:-${CI_COMMIT_REF_NAME:-main}}",
    "deploy:dndiy": "pnpm build:dndiy && find DNDIY.github.io/dist -size +25M -delete && pnpm dlx wrangler pages deploy DNDIY.github.io/dist --project-name merkin-dndiy --branch ${DEPLOY_BRANCH:-${CI_COMMIT_REF_NAME:-main}}",
    "deploy:temporal": "pnpm build:temporal && pnpm dlx wrangler pages deploy Temporal-Flow/dist --project-name merkin-temporal --branch ${DEPLOY_BRANCH:-${CI_COMMIT_REF_NAME:-main}}",
    "deploy:travel": "pnpm build:travel && pnpm dlx wrangler pages deploy apps/travel/dist --project-name merkin-travel --branch ${DEPLOY_BRANCH:-${CI_COMMIT_REF_NAME:-main}}",
    "deploy:all": "pnpm deploy:megameal && pnpm deploy:dndiy && pnpm deploy:temporal && pnpm deploy:travel",
    "lint:all": "pnpm -r lint",
    "format:all": "pnpm -r format",
    "type-check:all": "pnpm -r type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "overrides": {
      "svelte": "5.26.2"
    }
  }
}
