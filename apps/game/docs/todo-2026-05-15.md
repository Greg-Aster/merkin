# TODO 2026-05-15

Purpose: next-day handoff for the collision/readiness cleanup. The broad Schema v2 contract cleanup is done; tomorrow should stay target-driven and avoid reopening generic cleanup.

## Current State

- Readiness contract emits `schemaVersion: 2`.
- Runtime activation inputs are canonical under `runtime.*`.
- Deprecated top-level readiness mirrors are gone.
- Runtime-shaped `LevelBuildReport` mirrors are gone.
- Authored `runtimeAssets.requiredAssetActorIds` is rejected; use `runtimeAssets.requiredRenderActorIds`.
- `type-check`, `test:publish-pipeline`, `audit:collision`, and `git diff --check` passed after Schema v2.
- Observatory no longer uses `approvedHeightfieldException`; its GLB chunk terrain collision now declares source-linked projection provenance from the same source GLB contract.
- `audit:collision` now reports the remaining Observatory spawn note as `spawn-relies-on-terrain-collision`.
- Editor proxy collision has been removed. Mesh assets without authored runtime collision now resolve to no collider and surface a missing-collision diagnostic.
- Scene validation rejects removed editor proxy metadata: `collision.proxy`, `collision.bakeStatus`, and `collision.authoring`.

## Do First

1. Read `apps/game/docs/collision-pipeline-removal-scratchpad.md`.
2. Check current git status and identify other agents' active files before editing.
3. Do not revert unrelated dirty files.

## Priority Work

1. Observatory terrain replacement. DONE 2026-05-14.
   - Do not repeat the rejected generic `baked-heightfield` engine/schema rewrite.
   - Regenerated the Observatory terrain collider metadata as source-linked GLB projection (`source-glb-heightfield-projection`) instead of an approved heightfield exception.
   - Removed the `approvedHeightfieldException` / `authoredException` paths from runtime, validation, audits, scene data, terrain manifests, and runtime manifests.
   - Verification passed: `type-check`, `test:publish-pipeline`, `audit:collision`, `audit:engine`, `git diff --check`, and focused Observatory `profile:resources`.
   - Do not add a hidden spawn pad or broad budget bump just to silence future audit info.

2. Runtime smoke/profile follow-up. DONE 2026-05-14.
   - Removed the dead hidden `observatory-terrain-collider` source scene node that pointed at the 31 MB source GLB but was not cooked into the runtime actor list.
   - Focused desktop Observatory profile now passes and reaches playable runtime with `terrainColliders=1`.
   - Remaining profile output has non-strict performance warnings: pending GLTF cache entries, triangles slightly over desktop budget, long task max, and low RAF FPS.

3. Editor proxy removal. DONE 2026-05-14.
   - Removed fake editor proxy collider data from runtime/editor overlay/lifecycle paths.
   - Mesh assets without authored collision now remain colliderless in the editor and publish diagnostics report missing collision.
   - Removed `editorProxyCollision.ts`; bake scripts no longer read/write proxy metadata.
   - Level data check passed: current authored/cooked scenes contain no `proxy`, `bakeStatus`, or `authoring` collision metadata.

4. Remaining candidate work.
   - Observatory content/performance optimization: focused profile reaches playable with `terrainColliders=1`, but still reports non-strict warnings for pending GLTF cache entries, triangle budget, long task max, and low RAF FPS.
   - Runtime manifest validator simplification: keep it as a Node-safe adapter over shared readiness cores; remove more policy only with a focused diff and tests.
   - Audit hardening: add or confirm a regression gate that fails if removed proxy metadata reappears in authored or cooked scene files.

## Guardrails

- Keep runtime readiness logic in `levelRuntimeReadinessContractCore.mjs` and `evaluateLevelRuntimeActivation(...)`.
- Do not duplicate required render/collision/collider/terrain/spawn/world-partition/gameplay readiness in scripts or UI.
- Keep Observatory `source-linked-terrain-collision` and `ground.mode: "terrain-chunks"` aligned with the source-linked GLB projection manifest unless the level is migrated to a different explicit terrain authority.
- Do not reintroduce editor proxy compatibility fields. Missing mesh collision should stay missing until an authored primitive, explicit collider asset, or accepted terrain authority exists.

## Validation

Run after any code/data change:

```bash
pnpm --dir apps/game type-check
pnpm --dir apps/game test:publish-pipeline
pnpm --dir apps/game audit:collision
git diff --check
```

For Observatory/runtime work, also run when the dev server is healthy:

```bash
GAME_NO_SERVER=1 GAME_DEV_PORT=4322 pnpm --dir apps/game profile:resources --levels=observatory --profile=desktop-high-chromium-1080p --settle-ms=500
```

`audit:collision` may need an escalated rerun in this environment because `tsx` can fail to create its `/tmp/tsx-1000/*.pipe` IPC file.
