# Engine Contract Register

Status: current framework register
Last verified: 2026-06-06

This file is the single ownership map for `apps/game.megameal` engine contracts. It maps current engine contracts to their owners, writers, consumers, validation, and forbidden shortcuts.

Before changing a runtime, data, adapter, authoring, or validation contract, update the matching row here. If no row exists, add one before editing implementation. The goal is one source of truth per behavior, explicit consumers, repeatable validation, and no hidden runtime repair.

## Change Rule

Contract changes must be staged in this order:

1. Update the source-of-truth type, schema, or manifest.
2. Update the narrow runtime system or adapter that consumes it.
3. Update any game/content definitions using it.
4. Update durable validation: schemas, audits, or focused tests only when a reusable test owner exists.
5. Delete superseded compatibility code or record a removal condition.

Do not add fallback behavior as a substitute for moving the contract.

## Contract Table

| Contract | Source of truth | Allowed writers | Runtime consumers | Authoring/generated consumers | Validation | Forbidden shortcut | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `World` | `src/engine/core` | engine runtime, scene/prefab/level loaders, gameplay systems | all systems through `World` queries/resources | none | type-check, boundary audit | Svelte stores, Three objects, or Rapier bodies owning canonical gameplay state | Implemented |
| `Scheduler` | `src/engine/core`, `src/engine/runtime` | `EngineRuntime`, app mount wiring | input, commands, gameplay, physics, audio, render systems | none | type-check | relying on accidental registration order | Implemented |
| `CommandBus` | `src/engine/core` | input/UI/gameplay command producers | movement, replay, networking helpers | future replay/network tooling | type-check | direct cross-system mutation for intent | Implemented |
| `EventBus` | `src/engine/core` | gameplay/physics systems | audio, UI selectors, debug traces | future diagnostics | type-check | immediate hidden side effects with undefined timing | Implemented |
| `AssetManifest` | `src/engine/data/schemas`, local level asset data in `src/levels/<level>/data.json`, shared package assets in `src/levels/global`, player package assets in `src/levels/player`, and `src/engine/modules/assets` | level package data definitions and future content pipeline | `AssetManager`, `LevelLoader`, Three/audio loaders | future asset authoring/import tools; future durable generated asset manifests | asset manifest validator, `test:player-package-contract`, type-check | random imports scattered through gameplay systems, product asset manifests hidden in `src/game`, copied player assets in every level package, or a central asset file that forces unrelated level edits | Schema-hardened foundation implemented; shipped levels own local assets, shared/player assets import from their source packages, and runtime registers selected-manifest assets |
| `MaterialParameterContract` | `material` parameter data on material asset entries in `src/engine/data/schemas`, `src/levels/<level>/assets.ts`, and `src/levels/global` | checked-in level package asset definitions and future durable material import/generation owner | Three material loader and render adapter | future material editor/import tools; future cooked material manifests | asset manifest validator, `test:runtime-scene-contract`, type-check | renderer-side material mutation, URL-query material hacks as source of truth, shared material IDs that erase authored primitive differences, or old Svelte/Threlte material runtime repairs | Implemented foundation for checked-in Miranda primitive material parity; texture/shader/import tooling remains future |
| `PrefabDefinition` | `src/engine/data/schemas`, local level prefab data in `src/levels/<level>/data.json`, shared package prefabs in `src/levels/global/prefabs.ts`, player prefab data in `src/levels/player`, generic prefab registry in `src/game/prefabs` | level package content definitions and explicit migration code | `PrefabRegistry`, `LevelLoader` | future prefab authoring/import tools | prefab validator, `test:player-package-contract`, type-check | hardcoded entity construction in Svelte/Three/Rapier code, product prefab definitions hidden in `src/game`, copied player prefab data in each level, or one central prefab file that forces unrelated level edits | Implemented; shipped product prefab definitions live in level folders, `src/levels/global`, or the dedicated `src/levels/player` package, while `src/game/prefabs` owns only generic registry/spawn mechanics |
| `PlayerDataPackage` | `src/levels/player/data.json`, derived player package modules in `src/levels/player`, and per-level `player` config blocks in `src/levels/<level>/data.json` | checked-in player package data definitions and DEV-only `/editor` Player Package saves through `scripts/editor-dev-api.mjs`; per-level player override saves through the level workspace | `defineLevelPackage()`, `LevelLoader`, runtime scene readiness evaluation, audio content manifest composition | `/editor` Player Package panel for global player defaults and `/editor/level/` Player tab for per-level spawn/override config | `test:player-package-contract`, `test:runtime-scene-contract`, type-check | copying player assets, player prefab data, player audio mappings, or player instances into each level file; moving runtime player behavior into `src/levels`; hiding configurable player data in `src/game`; browser writes to TypeScript source instead of the player package data file | Implemented data-first player package; global prefab/assets/audio/movement/camera/collider/health/light defaults live in `src/levels/player/data.json`, derived package modules consume that file, and each level owns only spawn/override config |
| `LevelDefinition` | `src/engine/data/schemas`, `src/levels/<level>/data.json`, and resolved package data from `src/levels/levelPackageData.ts` | product level package definitions and explicit migration code | generic `LevelRegistry`/`LevelLoader` in `src/engine/modules/scene`, `SceneManager` | future editor/import tools | level validator, `test:player-package-contract`, type-check | one giant scene file, level data hidden in `src/game`, copied player instance data in every level, or level-id branches in engine/game code | Implemented data-first per-level owner foundation with imported player package composition |
| `SceneScope` | `src/engine/modules/scene` | `SceneManager`, scene loaders | scene cleanup, level assets, audio cleanup | future leak diagnostics | `test:runtime-scene-contract`, type-check | leaking listeners, assets, bodies, render objects, audio nodes, workers, or selected gameplay resources | Implemented; scene unload validation covers entity, selected interaction target, audio, and asset cleanup through the level-loader scope path |
| `RenderAdapter` | `src/engine/modules/rendering`, `src/engine/adapters/three` | renderer adapter only | render-sync/render stages | none | type-check, boundary audit | gameplay logic in `THREE.Object3D.userData` | Implemented |
| `SpriteRenderableContract` | `sprite` asset entries in `src/engine/data/schemas`, `Renderable.kind: "sprite"` in `src/engine/modules/rendering`, and checked-in level/global asset data under `src/levels` | level package data definitions and future sprite asset authoring/import tools | `AssetManager`, render sync, and the Three render adapter projection path only | future asset/NPC authoring tools through the same manifest and prefab data contracts | asset manifest validator, `test:npc-contract`, type-check, boundary audit | firefly-specific render systems, species branches in engine/adapters, mesh/material stand-ins for sprites, renderer-side gameplay state, `THREE.Object3D.userData` behavior channels, or direct scene insertion from NPC systems | Implemented generic sprite asset/renderable foundation for NPC visual parts; richer texture import/atlas tooling remains future |
| `AuthoredLightContract` | `Light` components in `src/engine/modules/rendering`, schema validation in `src/engine/data/schemas`, checked-in product prefab/level data in `src/levels/<level>` and `src/levels/global` | checked-in level package content definitions and future durable light import/generation owner | `LightSyncSystem`, Three renderer adapter, runtime scene readiness evaluation | future light editor/import tools; future cooked scene manifests | `test:runtime-scene-contract`, type-check, boundary audit | Svelte/Threlte light ownership, random/generated light IDs, render-profile stuffing for level-authored point lights, or old renderer-local light controllers copied into runtime | Implemented foundation for stable authored point lights; area/spot lights, shadows, and editor controls remain future |
| `PlayerCarriedLightContract` | Player light config in `src/levels/player` plus per-level `player.light` overrides in `src/levels/<level>/data.json`, `PlayerLightFeedback` state in `src/game/systems`, resolved runtime readiness from `src/levels/levelPackageData.ts`, and the design contract in `GAME_ENGINE_DESIGN_DOCUMENT.md` | checked-in level package definitions; game-owned charged-light feedback system; future player-light ability/follow systems | `LevelLoader`, player movement systems through the shared player `Transform`, charged action system state, `LightSyncSystem`, Three renderer adapter, runtime scene readiness evaluation | future light tuning/editor tooling; future release-pulse VFX and offset/follower authoring | `test:player-package-contract`, `test:runtime-scene-contract`, `test:charged-action-contract`, type-check, boundary audit | copying old Svelte/Threlte runtime lighting controllers, hidden renderer default lights, render-profile stuffing for player-local light, renderer-object brightness mutation, copied player light components in every level instance, or restoring old player-radius point-light culling | Implemented foundation for configured player-local point lights on the stable `player` entity plus held-charge intensity/distance feedback through engine `Light` state; offset rigs, shadows, and release-pulse tuning remain future |
| `PhysicsAdapter` | `src/engine/modules/physics`, `src/engine/adapters/rapier` | physics adapter and sync systems | physics pre/step/post stages, gameplay movement | none | type-check, boundary audit | gameplay code holding raw Rapier objects | Implemented |
| `InputActionMap` | `src/engine/modules/input`, `src/game/systems` | browser input adapter, gameplay input config | input and command systems | future input config tooling | `test:input-contract`, type-check | component-local browser event ownership or ambiguous primary-action branching | Implemented; gameplay actions explicitly cover movement, jump, sprint, held look, primary interaction, and light charge |
| `LookActivationContract` | `InputSnapshot.pointer`, `InputManager`, `BrowserInputAdapter` in `src/engine/modules/input` and `src/engine/adapters/browser` | browser input adapter and gameplay focus state only | `PlayerInputSystem`, `FirstPersonLookSystem`, HUD selectors | future input settings tooling | `test:input-contract`, type-check, boundary audit | Svelte-owned mouse state, camera-system mouse clamps, or pointer deltas emitted when held-look is inactive | Implemented foundation; pointer deltas are gated by held-look and disabled input clears stale device state |
| `ClickInteractionContract` | `InputSnapshot.pointer.clicks`, `InteractAtScreenPoint`, `InteractWithActiveTarget`, and `ActiveInteractionTarget` state in `src/game/systems` | browser input adapter for click candidates; player input system for commands; interaction target selection system for proximity arbitration | interaction command system, interaction target selection, HUD selector, portal activation, story-note activation, future interaction registry consumers | future editor interaction diagnostics | `test:input-contract`, `test:story-note-contract`, `test:runtime-scene-contract`, type-check | direct Svelte click mutation of gameplay, treating mouse-down as activation, coupling interaction to camera look, reading stale proximity resources after target arbitration, or allowing multiple active systems to consume one click/use event | Implemented foundation; small pointer releases become screen-point interaction commands, touch/gamepad active interaction emits active-target commands, nearest active portal/story-note target is selected consistently, HUD projects the selected target, activation systems consume the selected target, and scene unload clears selected target state |
| `ChargedActionContract` | `charge.light` action, `ChargedAction`, and `PlayerLightFeedback` game components in `src/game/systems` | player input system, charged action system, and game-owned charged-light feedback system | audio event mappings, player-carried light feedback, future ability/VFX consumers, and HUD selectors through semantic events/state | future ability authoring/import tooling | `test:charged-action-contract`, type-check | Svelte-local charge amount, direct audio/VFX calls from input, renderer-object light mutation, or copied old shockwave lifecycle arrays | Implemented foundation; charge component, system, semantic events, HUD projection, manifest-owned release SFX consumer, and held-charge player-light feedback exist |
| `NpcMovingActorContract` | `Npc` components in `src/game/systems`, level NPC data under `src/levels/<level>/npcs`, global archetype data under `src/levels/global/npcs`, and package composition in `src/levels/levelPackageData.ts` | checked-in level package NPC data and future approved editor NPC authoring tools | generic NPC proximity/dialog systems, HUD selector, movement and light-modulation systems | level editor NPC panels and future import/cook tools | `test:npc-contract`, `test:runtime-scene-contract`, type-check, boundary audit | firefly-specific runtime systems, species branches in engine/adapters, hiding moving actors in `src/game`, Svelte/Three ownership of NPC state, or non-player movers bypassing the NPC contract | Implemented V1 for authored Observatory firefly NPCs; all fireflies are NPCs and future authored non-player moving world objects should use this generic contract |
| `MovementBehaviorContract` | `MovementBehavior` components in level/prefab data, schema validation in `src/engine/data/schemas`, and generic movement processors in `src/game/systems/npcs.ts` | level package data and future movement authoring tools | fixed-step generic movement behavior system writing `Transform` before physics/render sync | future movement preset library and editor controls | `test:npc-contract`, type-check, boundary audit | per-species movement systems, per-entity timers/RAF loops, renderer-side transform animation, distance-based throttling of authored actors, or hardcoded movement constants outside data | Implemented V1 `static` and `hover-wander` data-driven movement |
| `LightModulationContract` | `LightModulation` components in level/prefab data, `Light` components, schema validation, and generic light-modulation processor in `src/game/systems/npcs.ts` | level package data and future light authoring tools | generic light modulation system, `LightSyncSystem`, Three renderer adapter | lighting workbench and future NPC editor controls | `test:npc-contract`, `test:runtime-scene-contract`, type-check | firefly-only light controllers, renderer-object brightness mutation, negative/NaN light writes, or limiting authored NPC lights by population or player distance | Implemented V1 pulse/blink modulation; authored Light components determine how many real lights exist and how far they reach |
| `NpcInteractionAndDialogContract` | `InteractionTarget`, `Conversation`, `ActiveNpc`, `OpenNpcDialog`, and `ActiveInteractionTarget` resources in `src/game/systems` | level package NPC data and game systems | generic NPC proximity/dialog systems and HUD projection | future conversation/editor tooling | `test:npc-contract`, `test:story-note-contract`, type-check | profile/AI conversation hidden in UI, NPC dialog outside command/event flow, or separate firefly interaction paths | Implemented V1 read-only NPC dialog using the existing command/event and HUD reader pattern |
| `MobileControlsContract` | `MobileInputControlsPort`, touch action IDs, and touch-look state in `src/engine/modules/input`, `src/game/systems/input.ts`, and `src/ui/MobileControls.svelte` | mobile UI input surface and browser input adapter only | `PlayerInputSystem`, `FirstPersonLookSystem`, interaction command system, charged action system | future input settings/mobile layout tooling | `test:input-contract`, type-check, boundary audit | mobile UI mutating player transform/camera/charge/portal state directly, raw browser touch identifiers becoming a second gameplay input path, or inheriting desktop pointer movement as mobile look | Implemented foundation; semantic touch controls feed engine input/contracts, raw target touch listeners are not gameplay input, and touch state clears on teardown |
| `AudioManifestAndEvents` | `src/engine/modules/audio`, `src/engine/adapters/browser/audio.ts`, local audio content mappings in `src/levels/<level>/data.json`, player audio defaults in `src/levels/player`, shared audio assets in `src/levels/global`, and `public/audio/ambient/AMBIENT_AUDIO_SOURCES.md` | audio adapter, asset manifest, audio content manifest | audio stage, scene cleanup, asset manager, runtime scene music and playlist application | future audio authoring/import tools; future durable generated audio manifests | audio content manifest validator, `test:player-package-contract`, `test:runtime-scene-contract`, type-check | direct Web Audio nodes in gameplay/UI code, hardcoded migrated sound maps outside content manifests, copied player audio mappings in every level, mapped sounds that are not preloaded/readiness-required, scene music or playlist entries that reference non-manifest assets, folder-scanning ambient playback, scene-scoped event mappings firing for inactive scenes, scene-ID audio switches in `src/game`, or copied old Svelte/Threlte audio components | Implemented foundation for event mappings, portal/Miranda scene music playlists, player package jump/charge SFX, shared portal activation SFX, file-backed collect SFX, active-scene filtering, and preload/readiness validation; local per-scene audio selection lives in each level folder and is routed through `src/levels/global/router.ts`; spatial listener/emitter sync and crossfades remain future |
| `RuntimeSceneManifest` | `src/engine/data/schemas`, `src/engine/data/manifests`, local runtime scene data in `src/levels/<level>/data.json`, imported player package data from `src/levels/player`, and the package router in `src/levels/global/router.ts` | checked-in prototype/authored manifest data; future durable content import/generation owner | `createGameScene`, `LevelLoader`, runtime scene readiness evaluation, runtime asset/prefab registration, app-level manifest selection, game runtime scene catalog | future manifest viewer/editor diagnostics; future generated scene manifests | runtime scene manifest validator, `test:player-package-contract`, `test:runtime-scene-contract`, type-check | runtime repair of missing authored data, loading from loose implicit component lists, default registry use when a manifest declares content, runtime scene IDs outside `src/levels`, copied player readiness data in every level, or hiding level catalogs inside `src/game` | Portal arena, prototype, Miranda deck, and Observatory foundation runtime loading implemented; each level owns local manifest data, `src/levels/player` composes player readiness, and `src/levels/global/router.ts` is the single package catalog/default-selection point |
| `LevelPackageRouter` | `src/levels/global/settings.ts`, `src/levels/global/router.ts`, and optional discovery bridge `src/app/levelPackageDiscovery.ts` | level package settings/router for installed package; app bridge for package discovery only | app mount, runtime default manifest selection, portal scene transitions, audio content lookup | future product configuration tooling | type-check, boundary audit | hardcoding default level policy inside engine core, app route branches, gameplay systems, or static imports that prevent deleting `src/levels` | Implemented foundation; default runtime scene is declared in level package settings, all level data routes through the package router, and the app bridge exposes an empty catalog when no level package is installed |
| `PerformanceConfigContract` | `src/game/performance`, `src/levels/global/performance.json`, per-level `src/levels/<level>/performance.json`, `src/levels/levelPackageData.ts`, DEV-only middleware in `scripts/editor-dev-api.mjs`, and editor performance panels | generic performance domain boundaries, scheduled runtime performance state, checked-in global/per-level performance config, DEV-only source edits, and authored `PerformanceLod` / `StreamingChunk` components in level/prefab data | `defineLevelPackage()` composes `game:performanceConfig` into runtime scene level resources; `createPerformanceRuntimeSystem()` reads the resource in game runtime; diagnostics read scheduled runtime state and world/resource counts; LOD, culling, streaming, and collision performance helpers consume compatible config/data without level branches | `/editor` Global Performance panel, `/editor/level/` Performance tab, performance diagnostic graph nodes, and live DEV bridge performance snapshots | `test:performance-config-contract`, `test:performance-runtime-contract`, `test:performance-lod-contract`, `test:performance-culling-contract`, `test:performance-streaming-contract`, `test:performance-collision-contract`, `test:performance-diagnostics-contract`, type-check, lint, boundary audit | product performance config hidden in `src/game`, editor-owned runtime state, hardcoded level IDs in performance systems, production file-write endpoints, overconstrained validation blockers, renderer/Rapier object mutation from performance policy code, direct asset unloads that bypass refcounts, or adapter-specific streaming shortcuts | Runtime foundation implemented: global and per-level JSON configs compose into runtime resources; the game runtime schedules `performance-runtime` and a cached `collision-spatial-index`; culling distance mode applies `Renderable.visible` and `Light.visible` through ECS; LOD distance mode evaluates configured tiers and swaps authored `PerformanceLod` renderable payloads; streaming plan mode emits operation diagnostics, retains/loads/releases `StreamingChunk.assetIds` through the asset manager, applies render/light visual residency, and removes/restores opt-in `Collider` components while retaining readiness-required colliders; collision spatial mode publishes bucket/query diagnostics and feeds walkable grounding candidate lookup; editor panels can save owning config files. Future work remains for entity spawn/despawn streaming, broader physics residency products, and performance-profiler systems. |
| `EditorGlobalSettingsWrite` | `src/levels/global/settings.ts`, dev-only middleware in `scripts/editor-dev-api.mjs`, and the `/editor` Global Settings panel | checked-in level package source edits and DEV-only editor save requests | app-level runtime settings discovery and `src/levels/global/router.ts` default manifest resolution | `/editor` global settings inspector and default-runtime-scene save action | type-check, lint, boundary audit, build | production file-write endpoints, runtime/editor imports across ownership boundaries, writing generated level data without an approved owner, or saving default scene IDs outside the level package settings file | Implemented initial DEV-only source edit path for `defaultRuntimeSceneId`; broader level editing must add explicit schemas and focused persistence contracts before writing more files |
| `LevelEditorWorkspaceV1` | `src/levels/<level>/data.json`, per-level `src/levels/<level>/skybox.json`, per-level `src/levels/<level>/performance.json`, `src/levels/levelPackageData.ts`, `src/editor/level/LevelEditorWorkspace.svelte`, `src/pages/editor/level.astro`, dev-only middleware in `scripts/editor-dev-api.mjs`, and `scripts/test-level-editor-workspace-contract.ts` | DEV-only editor save requests routed to owning checked-in level data files; checked-in wrapper modules consume generated data contracts | normal runtime consumes checked-in wrapper exports resolved through `defineLevelPackage()` after reload/rebuild | dedicated `/editor/level/` workspace opened from master-control level nodes; structured section editors for manifest, resources, instances, prefabs, assets, audio, render profile, skybox/environment data, and per-level performance config; readonly source inspection; asset upload helper; save-plus-live-reload command | `test:level-editor-workspace-contract`, `test:performance-config-contract`, `test:runtime-scene-contract`, `test:scene-environment-contract`, type-check, lint, boundary audit, build | raw arbitrary TypeScript writes from the browser, production file-write endpoints, editor-owned runtime state, bypassing level package ownership, saving broken cross-file references, hidden skybox/runtime repair fields, or treating viewport/gizmo editing as implemented | Implemented data-first workspace foundation for current runtime-consumed level package fields plus per-level skybox/environment and performance files; visual viewport picking/gizmos, terrain/collision cooking, sky import/cooked products, sky motion/chunk runtime, active performance optimization systems, and richer import tooling remain future |
| `RuntimeSceneTransition` | `RuntimeSceneTransitionPort` resource in `src/game/systems/components.ts`; checked-in runtime scene catalog routed through `src/levels/global/router.ts` | game runtime composition layer and checked-in portal level data | portal activation system, `SceneManager`, `LevelLoader`, renderer profile capability, physics/render cleanup | future map/level-select UI and editor preview tooling | `test:runtime-scene-contract`, type-check, boundary audit | app-shell level branches, direct Svelte scene loading, generated portal apparatus runtime code, or gameplay systems importing adapter APIs | Implemented foundation for manifest-ID scene transitions, including portal-arena outbound portals and the migrated Miranda airlock return portal targeting `observatory_runtime` |
| `MasterControlMap` | `src/editor/masterControlGraph.ts`, `src/editor/MasterControlMap.svelte`, `src/pages/editor.astro`, DEV-only editor middleware, and checked-in editor-owned data panels | optional editor/tooling surface and approved DEV-only data-source panels | none; normal game runtime must not import editor modules | architecture visualization window at `/editor`; Global Settings panel; Global Performance panel; Player Package panel; Game Runtime diagnostics panel; performance diagnostics graph nodes; future graph-driven diagnostics | type-check, lint, boundary audit, `test:performance-config-contract`, `test:performance-diagnostics-contract` | runtime imports from `src/editor`, production file-write endpoints, editor-owned runtime state, arbitrary TypeScript writes, or treating the map as a general level-save/publish editor | Implemented interactive architecture graph, Mermaid source projection, Global Settings save panel, Global Performance save panel, Player Package data panel, and live non-saved Game Runtime/performance diagnostics control with domain summaries |
| `LiveEditorDevBridge` | `src/app/gameDevBridge.ts`, live game snapshots from `src/app/GameClient.svelte`, scene control and diagnostic control exposed by `src/game/runtime`, runtime performance diagnostics under `src/game/performance`, and available levels routed through `src/levels/global/router.ts` | DEV-only app bridge and `/editor` command UI; game runtime validates scene-load requests through existing runtime scene control and owns live diagnostic toggles | local development game client only; normal production runtime does not require the bridge | `/editor` live session panel, active-level glow, command log, level-load command surface, collision-overlay diagnostic toggle, and runtime performance diagnostic readouts | type-check, lint, protected-doc audit, boundary audit, `test:collision-overlay-contract`, `test:performance-config-contract` | shipping the bridge as required gameplay infrastructure, importing `src/editor` from runtime/game/engine code, editor mutation of ECS/render/physics objects, bypassing `RuntimeSceneTransitionPort`, treating cached browser snapshots as source-of-truth level data, or persisting diagnostic toggles as level/global/player config | Implemented foundation for DEV-only browser-tab snapshots, cached editor reload state, editor-requested runtime scene switching, non-saved collision overlay diagnostics, and live performance counts/modes |
| `LegacyRuntimeCutover` | root `package.json` game scripts, `apps/game.megameal/src/pages/index.astro`, and this migration register | workspace script owners and `apps/game.megameal` app shell only | normal `dev:game`, `build:game`, `deploy:game`, and `dev:stack` game process selection | future deploy/CI tooling and eventual old-app archival work | root `build:game`, `apps/game.megameal` validation gate, diff check | importing `apps/game` runtime into `apps/game.megameal`, mounting old `ThreltGame` as the normal website runtime, or deploying `apps/game/dist` through normal game deploy scripts | Implemented root-script cutover; `apps/game` remains available only through explicit `:legacy` aliases and as source evidence for future contract migrations |
| `SaveReplayContract` | `src/engine/data/serializers`, `src/engine/data/replay` | serializer/replay helpers | debug/replay/network foundations | future save tooling; future save/replay artifacts | type-check | serializing Three, Rapier, DOM, GPU, or audio node objects | Implemented foundation |
| `NetworkingReadiness` | `src/engine/modules/networking` | future transport/session layer | replay/debug/network command helpers | future session diagnostics | type-check | trusting local-only state as a future multiplayer authority | Implemented foundation |
| `DebugContracts` | `src/engine/modules/debug` and runtime-owned diagnostics under `src/game/diagnostics` | debug systems/tools | inspector/profiler/trace/leak reports and runtime collision-overlay collection | future debug UI/export tools | type-check, `test:collision-overlay-contract` | making debug systems required gameplay dependencies, editor-side reads of ECS internals, or coupling diagnostics directly to Rapier adapter internals | Implemented foundation plus runtime-owned collision overlay collection for live diagnostics |
| `CollisionPolicy` | `Collider` and `RigidBody` components in prefab/level data; `Collider.intent` and `Collider.channel` schema validation | prefab/level definitions and future durable collision import/generation owner | physics sync and Rapier adapter | future collision editor/diagnostics; future collider products | schema collider checks, type-check | treating render meshes as collision without explicit collider data or repairing missing collision policy at runtime | Authored intent/channel foundation implemented for `solid`, `trigger`, and `walkable` collision on box, sphere, capsule, cylinder, and mesh collider shapes; mesh indices are schema-validated; collision masks/products still future |
| `WalkableCollisionContract` | `Collider.intent: "walkable"` plus `RuntimeSceneManifest.readiness.requiredWalkableStableIds` in `src/engine/data/schemas`, `src/engine/data/manifests`, per-level prefab/manifest data in `src/levels/<level>`, and generic walkable grounding in `src/game/systems/movement.ts` | checked-in level/prefab definitions and future durable collision import/generation owner | runtime scene readiness evaluation, game runtime physics pre-sync gate, generic walkable grounding, physics sync, Rapier adapter | future walkable-surface editor/import diagnostics; future cooked terrain collision pipeline | `test:runtime-scene-contract`, `test:static-environment-collision-contract`, type-check, boundary audit | treating render floors as implicit walkable surfaces, relying on old generated terrain collision manifests, hardcoding level IDs into movement, or broadening player bounds without authored walkable collision | Implemented for explicit walkable/worldStatic colliders with required stable IDs; generic walkable grounding now samples explicit walkable box/mesh collider data before kinematic character movement. Full Rapier character-controller collision resolution remains future |
| `ObservatoryCollisionContract` | `src/levels/observatory/collision/source.json`, generated product data in `src/levels/observatory/collision/generated.json`, Observatory level data in `src/levels/observatory/data.json`, and `observatory_runtime` readiness data, all consuming `CollisionPolicy`, `WalkableCollisionContract`, `LevelReadinessContract`, and `StaticEnvironmentCollisionContract` products | checked-in Observatory level package definitions plus `cook:static-environment-collision` for generated collision products | generic runtime scene readiness evaluation, generic walkable grounding, game runtime physics pre-sync gate, physics sync, Rapier adapter, player movement bounds | `/editor/level/` Collision tab, collision check/cook actions, and source/generated diagnostics; future collision preview/streaming authoring tools | `test:static-environment-collision-contract`, `test:runtime-scene-contract`, type-check, lint, boundary audit | treating a render asset ID such as `mesh_observatory_environment` as sufficient collision data, importing old terrain chunks/generated collider binaries, renderer or physics adapter repair of missing collision, water collision without `WaterSurfaceContract` volume data, or exposing Rapier internals to game content | Implemented generated static-environment foundation for the current Observatory GLB: level-owned walkable mesh collision chunks are generated from the declared source GLB, the generated hash is build-gated, player-spawn X/Z coverage is validated, the old flat `observatory:walkable-proxy` is removed from resolved level/readiness data, and four explicit boundary blockers remain |
| `StaticEnvironmentCollisionContract` | level-owned source configs and generated products under `src/levels/<level>/collision`, global static collision defaults/profiles in `src/levels/global/collisionSettings.ts`, `CollisionPolicy`, `RuntimeSceneManifest.readiness`, generic walkable grounding, and diagnostics under `src/game/diagnostics` | `cook:static-environment-collision`, `check:static-environment-collision`, checked-in authored/manual collision source configs, and future approved editor collision authoring tools | `defineLevelPackage()`, `LevelLoader`, generic walkable grounding, physics sync, Rapier adapter, runtime scene readiness, collision overlay diagnostics | `/editor/level/` Collision tab, source/generated diagnostics, and DEV-only check/cook actions; future collision preview/streaming authoring tools | `test:static-environment-collision-contract`, generated-product check mode, `test:level-editor-workspace-contract`, `test:runtime-scene-contract`, type-check, lint, boundary audit | renderer-derived implicit collision, adapter-side repair of missing authored collision, unvalidated mesh indices, dynamic bodies using triangle mesh collision by default, storing cooked collision outside the owning level package, regenerating products without source hashes, or manual collision mode without an explicit collision asset URL | Implemented V1 automatic GLB cook foundation: cook reads a level-owned source config, hashes the visual or explicit manual collision GLB, samples walkable source triangles into chunked mesh collider data, records emitted geometry/source diagnostics, composes chunks as explicit fixed/worldStatic mesh Collider instances, and build/editor checks report stale generated products. Runtime chunk streaming and non-walkable blocker extraction remain future |
| `PortalFieldTerrainContract` | `docs/PORTAL_FIELD_TERRAIN_INTENT.md`, portal terrain GLB asset in `public/assets/game/terrain`, `portalArenaAssetManifest`, `portal_arena_floor` prefab, and `portalArenaLevel` bounds under `src/levels/portal-arena` | checked-in level package content definitions and deterministic terrain generator script | `AssetManager`, `LevelLoader`, render sync, physics sync, readiness gate, player movement bounds | future terrain import/cook tooling and collision chunk pipeline | `test:runtime-scene-contract`, type-check, boundary audit | using GLB render geometry as implicit collision, hiding terrain defaults in the renderer, or expanding visuals without explicit movement/collision policy | Implemented foundation; portal arena uses a generated GLB visual field with explicit large flat collision proxy |
| `WaterSurfaceContract` | `docs/WATER_SURFACE_SYSTEM_PLAN.md`, shared water assets in `src/levels/global/waterAssets.ts`, shared water prefab in `src/levels/global/prefabs.ts`, per-level water instances in `src/levels/<level>/level.ts`, and manifest readiness data when required | checked-in reusable environment content definitions; future durable water import/cook tooling | `AssetManager`, `LevelLoader`, render sync, Three renderer adapter, runtime scene readiness evaluation; future water-volume/gameplay systems only through explicit contracts | future animated waves, reflections/refraction, rising water, underwater state, buoyancy, quality tiers, and water-body authoring tools | `test:runtime-scene-contract`, type-check, lint, boundary audit | making water an Observatory-only mesh/material owner, using render water as implicit collision, hiding water defaults in the renderer, importing old `apps/game` water/runtime/editor code, or adding water behavior without explicit data/components | Implemented V1 static visual foundation; Observatory consumes shared `mesh_water_plane`, `material_water_dark_still`, and `water_surface_plane` with no collider or gameplay volume |
| `ObservatoryLevelContract` | `docs/OBSERVATORY_PLAYABLE_FOUNDATION_PLAN.md`, `GAME_ENGINE_DESIGN_DOCUMENT.md`, Observatory asset/level/render/audio/manifest data in `src/levels/observatory`, authored Observatory NPC data in `src/levels/observatory/npcs`, and shared water/NPC data in `src/levels/global` | checked-in level package content definitions and explicit migration code rewritten from old scene evidence; future durable terrain/light/import tooling | `RuntimeSceneTransition`, `LevelLoader`, `AssetManager`, render sync, physics sync, audio stage, generic NPC/movement/light-modulation systems, `LightSyncSystem`, Three renderer adapter, runtime scene readiness evaluation | future cooked terrain collision chunks, generated NPC/firefly population tooling, water behavior through `WaterSurfaceContract`, reflections/post-processing, scene music selection, and generated scene manifest pipeline | `test:npc-contract`, `test:runtime-scene-contract`, `test:scene-environment-contract`, type-check, lint, boundary audit | importing or loading old `apps/game` runtime code, old generated runtime scene JSON, old terrain chunk runtime, generated collision binaries, Svelte/Threlte light ownership, render-profile stuffing for player/firefly local lights, hidden renderer defaults, old renderer-local light controllers, firefly-specific runtime systems, or making Observatory-specific water assets the reusable water owner | Implemented playable foundation includes target-engine assets/data, authored walkable floor and boundary proxy collision, shared static visual water instance, three authored firefly NPC moving actors with required light stable IDs and read-only dialog, portal transition by manifest ID, and focused readiness validation; full terrain collision chunks, dynamic water behavior, reflections/post-processing, scene music, generated NPC populations, and profile conversations remain future |
| `SpawnPoint` | stable level instance IDs and `Transform` overrides; future scene manifest spawn data | level definitions and explicit migration code | `LevelLoader`, prefab spawn, future playable gate | future editor spawn tools; future generated scene manifests | level validator, type-check | player spawn inferred from renderer or UI state or generated from level index | Authored stable IDs required for level instances |
| `RenderProfile` | `src/engine/data/schemas`, base per-level render profiles in `src/levels/<level>/data.json`, per-level skybox/environment data in `src/levels/<level>/skybox.json`, and Three adapter options | renderer adapter, asset manifest, future profile authoring | render sync/Three adapter and runtime scene transitions | level editor render and skybox panels; future generated scene manifests | render profile validator, `test:scene-environment-contract`, type-check | hidden runtime visual repair as a generic policy | Prototype, Miranda deck, portal arena, and Observatory profiles implemented; Three adapter can apply target scene profiles during transitions, with environment selection composed from per-level skybox data |
| `SkyboxEnvironmentContract` | `src/levels/<level>/skybox.json`, `RenderProfileData.environment`, environment asset entries in `src/engine/data/schemas`, checked-in shared environment assets in `src/levels/global/skyboxAssets.ts`, wrapper render profiles in `src/levels/<level>/renderProfile.ts`, architecture notes in `GAME_ENGINE_DESIGN_DOCUMENT.md`, `docs/Done/SKYBOX_CUBEMAP_SYSTEM_REVIEW.md`, `docs/Done/SCENE_ENVIRONMENT_FEATURE_PLAN.md`, and `docs/SKYBOX_FUTURE_FEATURES_IMPLEMENTATION_PLAN.md` | level package skybox data definitions and future durable sky/environment import pipeline | `defineLevelPackage()`, `AssetManager`, `LevelLoader` preload path, runtime scene readiness gate, game runtime scene transition path, Three renderer adapter scene-environment projection, reflection probe sync | `/editor/level/` Skybox tab; future environment editor/import tools, generated/cooked environment pipeline, quality tiers, richer reflection-probe blending/debugging, starmap/chunked-sky products, and future atmosphere/weather tooling | render profile/environment schema validation, cubemap face validation, `test:scene-environment-contract`, `test:runtime-scene-contract`, `test:level-editor-workspace-contract`, type-check, boundary audit | Svelte/Threlte component owning runtime sky state, generic Observatory fallback, gameplay systems hardcoding sky paths, renderer-side hidden default skyboxes, visible background and environment lighting being collapsed into hidden renderer defaults, or enabling planned starmap/chunk/motion fields before runtime support exists | Implemented foundation for per-level configurable skybox files feeding cubemap, equirectangular, muted video, procedural atmosphere, bounded dynamic capture, and authored reflection probes; current production scenes still use cubemap environments, while starmap/chunk/motion metadata remains planned-only until a focused runtime packet implements it |
| `InteractionRegistry` | `Collectible`, `Portal`, `StoryNote`, and `ActiveInteractionTarget` components/resources plus gameplay systems in `src/game/systems` | game systems/content and future interaction owner | collectible system, portal proximity/activation systems, story-note proximity/activation systems, interaction target selection, HUD selector, audio event mapping | future editor interaction tools; future cooked scene manifests; future screen-space/raycast selection services | `test:story-note-contract`, `test:runtime-scene-contract`, type-check | UI mutating inventory/progression directly, loading scenes directly, hardcoding story text in UI components, re-reading stale raw proximity state after selected-target arbitration, or letting overlapping proximity systems all consume the same interaction event | Collectibles, portal navigation, nearest active target arbitration, selected-target HUD projection, scene-unload selected-target cleanup, and StoryNote reader foundation implemented |
| `LevelReadinessContract` | `RuntimeSceneManifest.readiness`, `src/engine/data/manifests` readiness evaluator, and player readiness composition in `src/levels/levelPackageData.ts` | runtime scene manifests and imported player package data only | scene load/playable gate | future publish diagnostics; future cooked scene manifests | readiness evaluator, `test:player-package-contract`, `test:runtime-scene-contract`, type-check | enabling gameplay because a component happened to mount, or copying shared player readiness fragments into every level file | Runtime gate checks required assets, player stable ID, required collision prefabs, required collision stable IDs, required walkable stable IDs, required light stable IDs, physics readiness, and player readiness across the implemented portal arena, prototype, Miranda, and Observatory foundation scenes |

## Active Migration Status

| Packet | Status | Notes |
| --- | --- | --- |
| Contract register | Current | This root file is the canonical register. Do not recreate a duplicate under `docs/`. |
| Old-engine import ban | Implemented | `audit:engine-boundaries` rejects source imports/references into sibling `apps/game`. |
| Legacy runtime cutover | Implemented root-script cutover | Normal root scripts now run/deploy `@merkin/game-megameal`; the old `@merkin/game` app is retained behind explicit `:legacy` aliases for reference-only checks. |
| Runtime scene manifest loading | Portal arena, prototype, Miranda deck, and Observatory foundation implemented | Schema/types, narrow checked-in manifest data, loader helper, game-scene load path, required cubemap environment assets, and durable negative-case validation exist; no hidden boot preload, old runtime loader, or generated output path was copied. |
| Runtime manifest selection | Implemented foundation | The app may pass a checked-in runtime manifest into the client mount; the declared default is `portal_arena_runtime`, and portal navigation can request catalog scenes by manifest ID without editor/runtime repair. |
| Asset manifest schema hardening | Implemented | `AssetManager.registerManifest()` parses manifests before mutating maps, validates preload-group references and cubemap face definitions, permits exact idempotent re-registration for shared scene assets, and rejects conflicting asset/group definitions. Game runtime registration happens for the selected runtime scene manifest instead of a broad checked-in catalog. |
| Level readiness gate | Portal arena, prototype, Miranda deck, and Observatory foundation implemented | Readiness checks required assets, player spawn, collision prefabs, collision stable IDs, walkable stable IDs, light stable IDs, physics pre-sync collider readiness for required stable IDs, and player readiness before exposing the player resource. |
| Collision intent/channel validation | Implemented foundation | Authored `Collider` data must declare `intent` and `channel`; trigger intent must explicitly set `sensor: true`; solid and walkable intent cannot set `sensor: true`. Collision masks and cooked collision products remain future work. |
| Audio content manifest | Implemented curated track/SFX and playlist foundation | Event-to-sound mappings, portal arena scene music playlist, Miranda scene music playlist, collect/jump SFX, charge-release SFX, and portal activation SFX live in checked-in audio content manifests and validate against explicit audio assets. Runtime scenes preload and require mapped audio assets; spatial emitters, crossfades, and durable import/generation remain future. |
| Portal arena navigation room | Implemented field and player-light foundation | `portal_arena_runtime` owns eight portal slots, the player spawn at center, a required player-carried point light, a shared content-owned portal GLB, proximity prompts, click-to-transition support, and a generated GLB field with explicit collision proxy. |
| Water surface system | Implemented V1 static visual foundation | Water is a reusable environmental asset/prefab contract in `docs/WATER_SURFACE_SYSTEM_PLAN.md`, not an Observatory-only mesh/material owner. V1 provides a shared static visual surface with no collider or gameplay volume; future water behavior, reflections/refraction, volumes, and buoyancy extend the same contract. |
| Observatory playable foundation | Implemented foundation with generated static environment collision | `observatory_runtime` owns target-engine Observatory GLB art, generated level-owned walkable mesh collision chunks from the Observatory environment GLB, four required boundary blockers, a shared static water visual instance through `WaterSurfaceContract`, player-carried light, authored firefly NPC moving actors with required lights and read-only dialog, portal transition from the navigation room, and focused readiness validation, with no copied old runtime/editor systems. Runtime chunk streaming, non-walkable blocker extraction, dynamic water behavior, reflections/post-processing, scene music, generated NPC populations, and profile conversations remain future contracts. |
| Player controls migration | Implemented foundation | Held-look input gating, explicit gameplay action IDs, screen-point interaction command routing, nearest active target arbitration, selected-target HUD projection, scene-unload selected-target cleanup, portal click activation, mobile touch controls, light charge state/events, held-charge player-light feedback, focused input-contract validation, runtime-scene cleanup validation, and charged-action/story-note validation exist. Future work remains for screen-space/raycast inspect selection, release-pulse VFX, held-charge procedural audio, and expanded input settings. |
| Scene environment migration | Implemented foundation | `portal_arena_runtime`, `prototype_arena_runtime`, `miranda_deck_runtime`, and `observatory_runtime` declare manifest-backed cubemap environments. The completed feature packet in `docs/Done/SCENE_ENVIRONMENT_FEATURE_PLAN.md` adds schema/runtime/Three-adapter support for equirectangular, muted video, procedural atmosphere, bounded dynamic capture, and local reflection probes while preserving manifest ownership and renderer-only projection. Future packets are tracked in `docs/SKYBOX_FUTURE_FEATURES_IMPLEMENTATION_PLAN.md`. |
| Material parameter migration | Implemented foundation | Old Miranda primitive material values are migrated into schema-owned material asset data for already-migrated primitive prefabs. The implemented scope covers base color, emissive color/intensity, metalness, roughness, and Medbay opacity, with split material IDs where old authored primitive values differ. |
| Old content migration | Miranda deck, cockpit panel, crew quarters, Captain's Office, Engine Room, Miranda airlock return portal, Medbay, Mess Hall, Chapel altar, Brig, Cargo Hold, Archive primitive foundation, authored point-light foundation, and story-note reader foundation implemented | Checked-in target-engine data migrates the old Miranda spawn, two authored deck floors plus a Cargo Hold extension floor as required walkable collision surfaces, three cockpit glow panels, four crew bunks, the locker bank, Captain's Desk, Captain's Chair, Recipe Safe, Engine Core, four engine columns, the old airlock return portal targeting `observatory_runtime`, four Medbay pods, three Mess Hall blockers, Chapel Altar, four Brig cells, Brig Desk, four Cargo Hold stacks, five Archive server banks, three authored point lights, and nine StoryNote markers/readers as explicit prefabs/level instances plus gameplay-owned reader state with readiness requirements. Old generated story-marker and portal-apparatus GLBs remain excluded; full Miranda still needs broader terrain/cooked collision/import coverage and must not be marked ready. |

## Debt Rules

Compatibility code is allowed only when this file records:

- owner,
- reason,
- affected files,
- validation that prevents spread,
- removal condition.

If those fields are missing, the compatibility code is technical debt and should not be accepted.

## Current Validation Gate

```bash
pnpm --dir apps/game.megameal lint
pnpm --dir apps/game.megameal test:input-contract
pnpm --dir apps/game.megameal test:charged-action-contract
pnpm --dir apps/game.megameal test:story-note-contract
pnpm --dir apps/game.megameal test:scene-environment-contract
pnpm --dir apps/game.megameal test:runtime-scene-contract
pnpm --dir apps/game.megameal test:player-package-contract
pnpm --dir apps/game.megameal audit:engine-boundaries
pnpm --dir apps/game.megameal type-check
pnpm --dir apps/game.megameal build
git diff --check -- apps/game.megameal pnpm-lock.yaml
```

## Open Content And Scale Contracts

These are not blockers for the current playable framework, but they need owner decisions before larger content migration:

- Expanded authored GLB/gltf model library and compressed texture/mesh pipeline.
- Material texture/shader authoring, import, and generation beyond the checked-in Miranda primitive material parameter slice.
- Authored spatial listener/emitter sync, expanded audio library migration, music crossfades, held-charge procedural audio, and durable audio manifest import/generation beyond the current curated track/SFX foundation.
- Durable runtime scene manifest import/generation and physics-body readiness refinement.
- Area/spot light migration, light shadows, and editor light tooling beyond the authored point-light foundation.
- Reusable water surface assets/prefabs, water quality tiers, reflections/refraction, water volumes, rising water, buoyancy, and durable water-body import/cook tooling through `WaterSurfaceContract`; the first tracked plan is `docs/WATER_SURFACE_SYSTEM_PLAN.md`.
- Environment editor/import controls, generated/cooked environment pipelines, richer reflection-probe blending/debugging, fog/cloud/weather integration, and environment quality tiers building on the manifest-backed `SkyboxEnvironmentContract`; planned packets are tracked in `docs/SKYBOX_FUTURE_FEATURES_IMPLEMENTATION_PLAN.md`.
- Broader Miranda terrain/cooked collision readiness beyond the current checked-in walkable floor footprint.
- AAA-tier static environment collision products for detailed GLB environments, starting with Observatory.
- Async asset streaming, collider residency, physics adapter mutation, and performance-profiler systems that consume the `game:performanceConfig` resource without product-specific branches.
- Server-authoritative multiplayer session model.
- WebGPU/OffscreenCanvas worker renderer experiments.
- Threlte integration, only when there is a real runtime/editor consumer.
- Full editor migration from the old `apps/game` reference app.
