# Megameal Cleanup Log - 2026-06-10

This pass cleaned Megameal architecture without intentionally changing public features, routes, or user workflows.

## Removed

These files had no active source references in `apps/megameal/src` or `apps/megameal/public` at cleanup time:

- `src/components/widget/InfoCard.astro`
- `src/components/home/HomeIntroHeroSlide.astro`
- `src/components/home/HomeIntroLogoSprite.svelte`
- `src/components/home/HomeIntroLogoReflections.svelte`
- `src/components/home/HomeIntroTentacleModel.svelte`
- `src/components/home/ProceduralSnuggaloidScene.svelte`
- `src/components/home/homeIntroGlassTextures.ts`
- `src/components/home/LatestTransmissions.astro`
- `src/styles/features/extracted/latest-transmissions.css`
- `src/config/jerryConfig.ts`
- `src/config/password.config.ts`
- `src/utils/password.ts`
- `src/utils/starUtils.ts`

Notes:

- Site audio files, modules, and audio assets were not deleted.

Restored after build validation:

- `src/components/widget/RightRailContent.astro`
- `src/components/widget/SideBar.astro`

These are still imported through `packages/blog-core/src/layouts/MainGridLayout.astro`, so they are active compatibility shims for the shared layout.

## Centralized Contracts

New or expanded contract owners:

- `src/contracts/content.ts`: public content filtering, downloadable filtering, published-date sorting.
- `src/contracts/products.ts`: product visibility, lab prototype filtering, media, categories, hrefs, thumbnails, availability, and store search/sort data helpers.
- `src/contracts/archive.ts`: archive manifest public/restricted access check.
- `src/contracts/manifests.ts`: generated timeline/game manifest published-record check.

Routes and components now use these helpers instead of repeating draft/public filtering.

## Extracted Owners

Page and layout files were reduced by moving behavior or derivation into focused owners:

- `src/components/archive/archiveStoryMapModel.ts`
  - Owns archive story-map grouping, ordering, preview image fallback, access counts, labels, search text, and node positioning data.
- `src/components/archive/ArchiveStoryMap.astro`
  - Now mostly renders the archive story-map model output.
- `src/components/store/storefrontController.ts`
  - Owns storefront filtering/sorting, product detail tabs, and paginated product-card expansion.
- `src/layouts/store/storeLayoutChrome.ts`
  - Owns store layout parallax behavior.
- `src/components/home/portalScrollStages.ts`
  - Owns home portal scroll-stage behavior.
- `src/components/videos/videosArchiveRail.ts`
  - Owns video archive rail controls and overflow affordances.
- `src/components/cookbook/cookbookRecipeMode.ts`
  - Owns cookbook recipe-mode toggles and download active state.
- `src/utils/mainGridLayout.server.ts`
  - Owns `MainGridLayout.astro` route/layout policy helpers.
- `src/utils/archive-banner-video.ts`
  - Owns archive banner video playback-rate setup.

## CSS Changes

No new global style blocks were added.

Moved existing global style islands into explicit CSS owners:

- `src/styles/pages/pdf-post.css`
  - Took the PDF cookbook recipe-mode print/display rule from the PDF cookbook route.
- `src/styles/pages/cookbook-reader.css`
  - Took cookbook reader recipe-mode visibility and active download styling.
- `src/styles/pages/videos-index.css`
  - Took the video archive page banner/main-panel positioning rules.

Removed CSS:

- `src/styles/features/extracted/latest-transmissions.css`, because its only importer was the removed `LatestTransmissions.astro`.

Still intentionally left for a later focused review:

- `src/components/cookbook/CookbookBanner.astro` still has a global style block that reaches into `#banner-container.has-cookbook-banner`. It should be moved only with a banner/layout ownership pass.

## Validation

Completed validation:

- `pnpm --dir apps/megameal exec biome format --write ...`
- `pnpm --dir apps/megameal type-check`
- `pnpm --dir apps/megameal audit:contracts`
- `pnpm --dir apps/megameal audit:css:changed`
- `pnpm --dir apps/megameal audit:css:strict`
- `pnpm --dir apps/megameal build`
- `git diff --check -- apps/megameal`

Build notes:

- The final build completed successfully.
- Existing warnings remain for PostCSS `from`, Vite circular chunks, and large chunks.
- A first build attempt caught that `RightRailContent.astro` and `SideBar.astro` were still imported through `packages/blog-core`; both files were restored and documented above.

Browser smoke checks were not run as part of this pass, per repository instructions.
