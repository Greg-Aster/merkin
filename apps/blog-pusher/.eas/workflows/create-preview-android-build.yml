name: Create Preview Android Build

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android preview APK
    type: build
    params:
      platform: android
      profile: preview
